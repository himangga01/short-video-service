# 🚀 SSUL MAKER - 간소화된 개발 로드맵 (인증 제외)

## 📌 개요
- **Frontend**: React + TypeScript
- **Backend**: Node.js + Express
- **Database**: SQLite
- **인증**: 제외 (나중에 추가)

---

## 📍 Phase 0: 프로젝트 초기 설정 (1-2일)

### ✅ 완료해야 할 작업

#### 1. 프로젝트 구조 생성
```bash
mkdir ssul-maker
cd ssul-maker
git init
```

#### 2. Frontend 초기화
```bash
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install

# 핵심 라이브러리만 설치
npm install react-router-dom @reduxjs/toolkit react-redux
npm install axios socket.io-client
npm install tailwindcss @radix-ui/react-dialog lucide-react
npm install @dnd-kit/core @dnd-kit/sortable
npm install wavesurfer.js
```

#### 3. Backend 초기화
```bash
cd ../
mkdir backend && cd backend
npm init -y

# 핵심 라이브러리만 설치
npm install express cors dotenv
npm install better-sqlite3 knex
npm install multer sharp
npm install socket.io
npm install p-queue node-cache
npm install -D nodemon
```

#### 4. 데이터베이스 설정 (인증 제외)
```bash
# 마이그레이션 생성 (Users 테이블 제외)
npx knex migrate:make create_projects  
npx knex migrate:make create_panels
npx knex migrate:make create_presets

# 마이그레이션 실행
npx knex migrate:latest
```

---

## 🚀 Phase 1: MVP 핵심 기능 (1.5주)

### Week 1: 프로젝트 및 패널 관리

#### Day 1-2: 프로젝트 CRUD
- [ ] Project 테이블 마이그레이션 (user_id 제외)
```sql
CREATE TABLE projects (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  description TEXT,
  channel_name TEXT,
  view_count INTEGER DEFAULT 0,
  status TEXT DEFAULT 'draft',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

- [ ] 프로젝트 CRUD API
  - `GET /api/projects` - 모든 프로젝트 조회
  - `POST /api/projects` - 프로젝트 생성
  - `GET /api/projects/:id` - 프로젝트 상세 조회
  - `PUT /api/projects/:id` - 프로젝트 수정
  - `DELETE /api/projects/:id` - 프로젝트 삭제

**핵심 파일:**
```javascript
// backend/src/routes/projects.js
const express = require('express');
const router = express.Router();
const db = require('../config/database');

// 모든 프로젝트 조회
router.get('/', async (req, res) => {
  try {
    const projects = await db('projects')
      .select('*')
      .orderBy('created_at', 'desc');
    res.json({ data: projects });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

// 프로젝트 생성
router.post('/', async (req, res) => {
  try {
    const { title, description, channel_name } = req.body;
    const [id] = await db('projects').insert({
      id: generateId(),
      title,
      description,
      channel_name,
    });
    res.status(201).json({ data: { id, title } });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

module.exports = router;
```

#### Day 3-4: 패널 CRUD
- [ ] Panel 테이블 마이그레이션
```sql
CREATE TABLE panels (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  order_index INTEGER NOT NULL,
  script TEXT NOT NULL,
  image_url TEXT,
  audio_url TEXT,
  voice_id TEXT DEFAULT 'Jiny',
  voice_speed REAL DEFAULT 1.0,
  text_size INTEGER DEFAULT 22,
  text_color TEXT DEFAULT '#FFFFFF',
  background_color TEXT DEFAULT '#000000',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);
```

- [ ] 패널 CRUD API
  - `GET /api/projects/:projectId/panels` - 패널 목록
  - `POST /api/projects/:projectId/panels` - 패널 생성
  - `PUT /api/panels/:id` - 패널 수정
  - `DELETE /api/panels/:id` - 패널 삭제
  - `PATCH /api/panels/reorder` - 순서 변경

**핵심 파일:**
- `backend/src/routes/panels.js`
- `backend/src/controllers/panelController.js`

#### Day 5: 기본 UI 레이아웃
- [ ] App 라우팅 설정 (로그인 페이지 제외)
```typescript
// frontend/src/App.tsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Dashboard from './pages/Dashboard';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Dashboard />} />
        <Route path="/project/:id" element={<Dashboard />} />
      </Routes>
    </BrowserRouter>
  );
}
```

- [ ] Redux Store 설정 (auth 제외)
```typescript
// frontend/src/store/store.ts
import { configureStore } from '@reduxjs/toolkit';
import projectReducer from './slices/projectSlice';
import panelReducer from './slices/panelSlice';
import uiReducer from './slices/uiSlice';

export const store = configureStore({
  reducer: {
    projects: projectReducer,
    panels: panelReducer,
    ui: uiReducer,
  },
});
```

### Week 2: UI 구현 및 드래그 앤 드롭

#### Day 1-2: 3-Column 레이아웃
- [ ] 좌측 사이드바 (Sidebar)
  - SSUL MAKER 로고
  - **+ 새 프로젝트** 버튼
  - 프로젝트 리스트
  - 저장 버튼

```typescript
// frontend/src/components/layout/Sidebar.tsx
export default function Sidebar() {
  const projects = useAppSelector(state => state.projects.list);
  const dispatch = useAppDispatch();

  const handleNewProject = () => {
    dispatch(createProject({ title: '새 프로젝트' }));
  };

  return (
    <div className="w-60 bg-black h-screen flex flex-col p-4">
      {/* 로고 */}
      <h1 className="text-red-600 text-xl font-bold mb-6">SSUL MAKER</h1>
      
      {/* 새 프로젝트 버튼 */}
      <button 
        onClick={handleNewProject}
        className="w-full bg-red-600 text-white py-2 px-4 rounded mb-4"
      >
        + 새 프로젝트
      </button>

      {/* 프로젝트 리스트 */}
      <div className="flex-1 overflow-y-auto">
        {projects.map(project => (
          <ProjectCard key={project.id} project={project} />
        ))}
      </div>

      {/* 저장 버튼 */}
      <button className="w-full bg-red-600 text-white py-2 px-4 rounded mt-4">
        저장
      </button>
    </div>
  );
}
```

- [ ] 중앙 패널 (CenterPanel)
  - 채널명 헤더
  - 프로젝트 정보
  - 프로젝트 본문

- [ ] 우측 패널 (RightPanel)
  - 패널 설정 탭
  - 스크립트 편집 / 이미지 적용

**핵심 파일:**
- `frontend/src/components/layout/Sidebar.tsx`
- `frontend/src/components/layout/CenterPanel.tsx`
- `frontend/src/components/layout/RightPanel.tsx`

#### Day 3-4: 패널 편집 UI + 드래그 앤 드롭
- [ ] PanelList 컴포넌트 (드래그 앤 드롭)
```typescript
// frontend/src/components/panel/PanelList.tsx
import { DndContext, closestCenter } from '@dnd-kit/core';
import { SortableContext, verticalListSortingStrategy } from '@dnd-kit/sortable';

export default function PanelList({ panels }) {
  const handleDragEnd = (event) => {
    // 순서 변경 로직
    dispatch(reorderPanels({ oldIndex, newIndex }));
  };

  return (
    <DndContext collisionDetection={closestCenter} onDragEnd={handleDragEnd}>
      <SortableContext items={panels} strategy={verticalListSortingStrategy}>
        {panels.map((panel) => (
          <PanelItem key={panel.id} panel={panel} />
        ))}
      </SortableContext>
    </DndContext>
  );
}
```

- [ ] PanelItem 컴포넌트
  - 패널 번호
  - 스크립트 텍스트 영역
  - 음성 설정 (Jiny, 속도)
  - 스타일 설정 (배경색, 글자색, 크기)
  - 이미지 추가 버튼
  - 삭제 버튼

**핵심 파일:**
- `frontend/src/components/panel/PanelList.tsx`
- `frontend/src/components/panel/PanelItem.tsx`

#### Day 5: 통합 테스트
- [ ] API 연동 확인
- [ ] 드래그 앤 드롭 동작 확인
- [ ] 버그 수정

**Phase 1 완료 기준:**
- ✅ 프로젝트 생성/조회/수정/삭제 가능
- ✅ 패널 추가/편집/삭제 가능
- ✅ 패널 순서 변경 (드래그 앤 드롭) 가능
- ✅ 3-Column UI 레이아웃 완성
- ✅ 데이터가 SQLite에 저장됨

---

## 🎨 Phase 2: 핵심 기능 고도화 (3주)

### Week 1: 파일 업로드 및 TTS

#### Day 1-2: 이미지 업로드
- [ ] Multer 설정
- [ ] 이미지 업로드 API
```javascript
// backend/src/routes/upload.js
const multer = require('multer');
const sharp = require('sharp');

const upload = multer({
  storage: multer.memoryStorage(),
  limits: { fileSize: 10 * 1024 * 1024 } // 10MB
});

router.post('/images', upload.single('image'), async (req, res) => {
  try {
    // Sharp로 이미지 처리
    const optimized = await sharp(req.file.buffer)
      .resize(1920, 1080, { fit: 'inside' })
      .webp({ quality: 80 })
      .toBuffer();
    
    // 파일 저장
    const filename = `${Date.now()}.webp`;
    const filepath = path.join('uploads/images', filename);
    await fs.writeFile(filepath, optimized);
    
    res.json({ 
      url: `/uploads/images/${filename}`,
      size: optimized.length 
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
```

- [ ] 이미지 업로드 UI
```typescript
// frontend/src/components/panel/ImageUpload.tsx
export default function ImageUpload({ panelId }) {
  const handleUpload = async (file: File) => {
    const formData = new FormData();
    formData.append('image', file);
    
    const { data } = await axios.post('/api/upload/images', formData);
    
    // 패널에 이미지 URL 저장
    dispatch(updatePanel({ 
      id: panelId, 
      image_url: data.url 
    }));
  };

  return (
    <div className="border-2 border-dashed border-gray-300 p-4 rounded">
      <input 
        type="file" 
        accept="image/*" 
        onChange={(e) => handleUpload(e.target.files[0])}
      />
    </div>
  );
}
```

#### Day 3-5: Gemini TTS 통합
- [ ] Gemini API 설정
```javascript
// backend/src/services/ttsService.js
const fs = require('fs/promises');
const path = require('path');

async function generateTTS(text, voiceId = 'Kore', speed = 1.0) {
  const pcm = await requestGeminiTTS({
    model: 'gemini-2.5-flash-preview-tts',
    voiceName: voiceId,
    text,
    speed,
  });
  const buffer = await convertPcmToMp3(pcm);
  
  // 파일 저장
  const filename = `tts-${Date.now()}.mp3`;
  const filepath = path.join('uploads/audio', filename);
  await fs.writeFile(filepath, buffer);
  
  return `/uploads/audio/${filename}`;
}

module.exports = { generateTTS };
```

- [ ] TTS 생성 API
```javascript
// backend/src/routes/tts.js
router.post('/generate', async (req, res) => {
  const { panelId, text, voiceId, speed } = req.body;
  
  try {
    const audioUrl = await ttsService.generateTTS(text, voiceId, speed);
    
    // 패널에 오디오 URL 저장
    await db('panels')
      .where({ id: panelId })
      .update({ audio_url: audioUrl });
    
    res.json({ audioUrl });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
```

- [ ] 작업 큐 통합 (p-queue)
```javascript
// backend/src/services/queueService.js
const PQueue = require('p-queue').default;

const ttsQueue = new PQueue({ concurrency: 3 });

async function addTTSJob(jobData) {
  return ttsQueue.add(async () => {
    return await ttsService.generateTTS(
      jobData.text, 
      jobData.voiceId, 
      jobData.speed
    );
  });
}

module.exports = { addTTSJob };
```

### Week 2: 오디오 플레이어

#### Day 1-3: 오디오 플레이어 구현
- [ ] WaveSurfer.js 통합
```typescript
// frontend/src/components/audio/AudioPlayer.tsx
import { useEffect, useRef, useState } from 'react';
import WaveSurfer from 'wavesurfer.js';

export default function AudioPlayer({ audioUrl }) {
  const waveformRef = useRef(null);
  const wavesurfer = useRef(null);
  const [isPlaying, setIsPlaying] = useState(false);

  useEffect(() => {
    if (waveformRef.current && audioUrl) {
      wavesurfer.current = WaveSurfer.create({
        container: waveformRef.current,
        waveColor: '#999',
        progressColor: '#FF0000',
        height: 60,
      });

      wavesurfer.current.load(audioUrl);
    }

    return () => {
      wavesurfer.current?.destroy();
    };
  }, [audioUrl]);

  const handlePlayPause = () => {
    wavesurfer.current?.playPause();
    setIsPlaying(!isPlaying);
  };

  return (
    <div className="audio-player">
      <div ref={waveformRef} />
      <button onClick={handlePlayPause}>
        {isPlaying ? '⏸️' : '▶️'}
      </button>
    </div>
  );
}
```

- [ ] 패널별 오디오 재생
- [ ] 전체 재생 기능

#### Day 4-5: TTS 진행률 표시 (WebSocket)
- [ ] Socket.io 서버 설정
```javascript
// backend/src/socket/index.js
const socketIO = require('socket.io');

function initializeSocket(server) {
  const io = socketIO(server, {
    cors: { origin: process.env.FRONTEND_URL }
  });

  io.on('connection', (socket) => {
    console.log('Client connected:', socket.id);

    socket.on('disconnect', () => {
      console.log('Client disconnected:', socket.id);
    });
  });

  return io;
}

module.exports = { initializeSocket };
```

- [ ] TTS 진행률 이벤트
```javascript
// TTS 생성 중 진행률 전송
io.emit('tts:progress', {
  panelId: panelId,
  progress: 50,
  message: 'TTS 생성 중...'
});

io.emit('tts:completed', {
  panelId: panelId,
  audioUrl: audioUrl
});
```

- [ ] 진행률 모달 UI
```typescript
// frontend/src/components/tts/TTSProgressModal.tsx
export default function TTSProgressModal({ isOpen, progress }) {
  return (
    <Dialog open={isOpen}>
      <DialogContent>
        <h2>TTS 생성 중...</h2>
        <div className="w-full bg-gray-200 rounded">
          <div 
            className="bg-red-600 h-2 rounded transition-all"
            style={{ width: `${progress}%` }}
          />
        </div>
        <p>{progress}%</p>
      </DialogContent>
    </Dialog>
  );
}
```

### Week 3: 자동 저장

#### Day 1-3: 자동 저장 구현
- [ ] Debounce Hook
```typescript
// frontend/src/hooks/useDebounce.ts
import { useEffect, useState } from 'react';

export function useDebounce<T>(value: T, delay: number): T {
  const [debouncedValue, setDebouncedValue] = useState<T>(value);

  useEffect(() => {
    const timer = setTimeout(() => {
      setDebouncedValue(value);
    }, delay);

    return () => {
      clearTimeout(timer);
    };
  }, [value, delay]);

  return debouncedValue;
}
```

- [ ] 자동 저장 로직
```typescript
// frontend/src/hooks/useAutoSave.ts
export function useAutoSave(data, saveFn) {
  const debouncedData = useDebounce(data, 3000); // 3초 딜레이

  useEffect(() => {
    if (debouncedData) {
      saveFn(debouncedData);
      console.log('자동 저장됨:', new Date().toLocaleTimeString());
    }
  }, [debouncedData, saveFn]);
}
```

- [ ] 저장 상태 표시
```typescript
// "저장 중..." / "저장됨" 표시
const [saveStatus, setSaveStatus] = useState('저장됨');

useEffect(() => {
  setSaveStatus('저장 중...');
  // ... 저장 로직
  setSaveStatus('저장됨');
}, [debouncedData]);
```

#### Day 4-5: 통합 및 테스트
- [ ] 전체 플로우 테스트
- [ ] 버그 수정

**Phase 2 완료 기준:**
- ✅ 이미지 업로드 및 표시 가능
- ✅ TTS 생성 및 오디오 재생 가능
- ✅ 실시간 진행률 표시
- ✅ 3초마다 자동 저장

---

## ⚡ Phase 3: 고급 기능 추가 (2주)

### Week 1: 프리셋 및 검색

#### Day 1-2: 프리셋 시스템
- [ ] Preset 테이블
```sql
CREATE TABLE presets (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT,
  thumbnail_url TEXT,
  data TEXT NOT NULL, -- JSON 데이터
  usage_count INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

- [ ] 프리셋 API
- [ ] 프리셋 UI (드롭다운)

#### Day 3-4: 검색 및 필터
- [ ] 프로젝트 검색 API
- [ ] 검색 바 UI
- [ ] 필터 옵션 (날짜, 상태)

#### Day 5: AI 대본 입력 모달
- [ ] 대용량 텍스트 입력 모달
- [ ] 자동 패널 분할 (문장 단위)
```typescript
function splitTextIntoPanels(text: string): string[] {
  // 마침표, 느낌표, 물음표로 분할
  const sentences = text.match(/[^.!?]+[.!?]+/g) || [];
  return sentences.map(s => s.trim());
}
```

### Week 2: 편의 기능

#### Day 1-2: Undo/Redo
- [ ] History 스택 구현
- [ ] Ctrl+Z / Ctrl+Y 단축키

#### Day 3-4: 패널 복사/붙여넣기
- [ ] 클립보드 Hook
- [ ] Ctrl+C / Ctrl+V 지원
- [ ] 컨텍스트 메뉴

#### Day 5: 통합 테스트

**Phase 3 완료 기준:**
- ✅ 프리셋 적용 가능
- ✅ 검색 동작
- ✅ AI 대본 입력 → 자동 분할
- ✅ Undo/Redo 동작
- ✅ 복사/붙여넣기 가능

---

## 🏁 Phase 4: 최적화 및 배포 (1주)

### Day 1-2: 성능 최적화
- [ ] React.lazy로 Code Splitting
- [ ] 이미지 최적화 (lazy loading)
- [ ] 메모이제이션 (React.memo)

### Day 3-4: 배포 준비
- [ ] 환경 변수 설정
- [ ] Production 빌드
- [ ] Frontend 배포 (Vercel)
- [ ] Backend 배포 (Heroku/Railway)

### Day 5: 문서화
- [ ] README.md 작성
- [ ] 사용자 가이드

---

## 📊 전체 타임라인

```
Week 1-2:   Phase 0 + Phase 1 (프로젝트/패널 CRUD + UI)
Week 3-5:   Phase 2 (파일 업로드, TTS, 오디오)
Week 6-7:   Phase 3 (프리셋, 검색, 편의 기능)
Week 8:     Phase 4 (최적화, 배포)
```

**예상 총 기간: 약 2개월**

---

## 🎯 다음 단계 (지금 바로 시작)

### 1. 프로젝트 생성
```bash
mkdir ssul-maker
cd ssul-maker

# Frontend
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
npm install react-router-dom @reduxjs/toolkit react-redux axios

# Backend
cd ..
mkdir backend && cd backend
npm init -y
npm install express cors dotenv better-sqlite3 knex
npm install -D nodemon
```

### 2. 데이터베이스 마이그레이션 생성
```bash
cd backend
npx knex migrate:make create_projects
npx knex migrate:make create_panels
```

### 3. 첫 API 구현
```javascript
// backend/src/app.js
const express = require('express');
const cors = require('cors');

const app = express();
app.use(cors());
app.use(express.json());

app.get('/health', (req, res) => {
  res.json({ status: 'ok' });
});

app.listen(3001, () => {
  console.log('Server running on http://localhost:3001');
});
```

---

## 🎬 2026-05-04 개정: 인증 제외 영상 편집 MVP 로드맵

인증은 계속 제외하되, MVP의 중심을 프로젝트/패널 CRUD에서 영상 제작 흐름으로 변경합니다. 목표는 로컬 환경에서 하나의 썰 영상을 끝까지 만들어 mp4로 export하는 것입니다.

### Step 1: 안정화

- [ ] 깨진 한글 문구 수정
- [ ] `npm run lint` 통과
- [ ] `npm run build` 통과
- [ ] `better-sqlite3` 재빌드 또는 재설치
- [ ] 패널 reorder 라우트 수정

### Step 2: 편집 작업대 UI

- [ ] 중앙 `VideoPreviewCanvas` 구현
- [ ] 하단 `SceneTimeline` 구현
- [ ] 우측 `SceneInspector` 탭 구조 적용
- [ ] 상단 `EditorToolbar` 구현
- [ ] UI 용어를 패널에서 씬 중심으로 정리

### Step 3: 이미지 적용

- [ ] `uploads/images` 저장 경로 정리
- [ ] 이미지 업로드 API 구현
- [ ] Sharp로 WebP 변환/썸네일 생성
- [ ] 씬에 이미지 연결
- [ ] 프리뷰 캔버스에 이미지 표시

### Step 4: TTS 생성

- [ ] Gemini API 연동
- [ ] TTS 환경변수 추가
- [ ] `ttsService` 구현
- [ ] TTS hash 캐시 구현
- [ ] 씬 단위 TTS 생성 API 구현
- [ ] mp3 파일 저장
- [ ] duration 계산
- [ ] 프론트 오디오 플레이어 연결

### Step 5: 전체 재생

- [ ] 씬별 duration 기반 전체 길이 계산
- [ ] 전체 재생 버튼
- [ ] playhead와 프리뷰 동기화
- [ ] 현재 재생 씬 하이라이트

### Step 6: Export

- [ ] FFmpeg 설치/실행 확인
- [ ] 렌더 입력 JSON 생성
- [ ] 이미지+오디오+자막 클립 생성
- [ ] 씬 concat
- [ ] mp4 다운로드 URL 제공
- [ ] 렌더 실패/재시도 UX 추가

### 인증 제외 상태에서 보류할 것

- 로그인/회원가입
- 팀/협업
- 결제/사용량 제한
- 클라우드 저장소
- 공유 링크

---

**인증 제외로 개발 기간이 약 1주 단축되었습니다!** 🚀

나중에 필요할 때 인증 시스템을 추가할 수 있습니다.

