# 🚀 SSUL MAKER - 구현 로드맵

## 📌 문서 정보
- **프로젝트명**: SSUL MAKER (썰 메이커)
- **작성일**: 2025-10-06
- **문서 유형**: 단계별 구현 가이드
- **기반 문서**: 요구사항서, 설계서, UX/UI 기능설명서, 기술스택 명세서, 개선 필요사항 분석

---

## 목차
1. [전체 구현 개요](#1-전체-구현-개요)
2. [Phase 0: 프로젝트 초기 설정](#phase-0-프로젝트-초기-설정)
3. [Phase 1: MVP 핵심 기능](#phase-1-mvp-핵심-기능)
4. [Phase 2: 핵심 기능 고도화](#phase-2-핵심-기능-고도화)
5. [Phase 3: 고급 기능 추가](#phase-3-고급-기능-추가)
6. [Phase 4: 최적화 및 배포](#phase-4-최적화-및-배포)
7. [각 Phase별 상세 작업](#각-phase별-상세-작업)

---

## 1. 전체 구현 개요

### 1.1 구현 전략

```
┌─────────────────────────────────────────────────────────┐
│              SSUL MAKER 구현 전략                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Phase 0: 프로젝트 초기 설정 (1-2일)                     │
│  ├─ 프로젝트 구조 생성                                   │
│  ├─ 개발 환경 설정                                       │
│  └─ 데이터베이스 스키마 구축                              │
│                                                          │
│  Phase 1: MVP 핵심 기능 (2주)                            │
│  ├─ 인증 시스템                                          │
│  ├─ 프로젝트 CRUD                                        │
│  ├─ 패널 편집 기본 기능                                  │
│  └─ 기본 UI 구현                                         │
│                                                          │
│  Phase 2: 핵심 기능 고도화 (3주)                         │
│  ├─ TTS 통합                                            │
│  ├─ 파일 업로드                                          │
│  ├─ 오디오 플레이어                                      │
│  └─ 실시간 자동 저장                                     │
│                                                          │
│  Phase 3: 고급 기능 추가 (4주)                           │
│  ├─ 프리셋 시스템                                        │
│  ├─ 검색 및 필터                                         │
│  ├─ Undo/Redo                                           │
│  └─ 발음 사전                                            │
│                                                          │
│  Phase 4: 최적화 및 배포 (2주)                           │
│  ├─ 성능 최적화                                          │
│  ├─ 테스트 작성                                          │
│  ├─ 배포 준비                                            │
│  └─ 문서화                                               │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 1.2 우선순위 기준

#### 🔴 Critical (Phase 1 - MVP)
- 사용자 인증
- 프로젝트 생성/조회
- 패널 추가/편집
- 기본 UI 레이아웃

#### 🟠 High (Phase 2)
- TTS 생성
- 이미지 업로드
- 오디오 재생
- 자동 저장

#### 🟡 Medium (Phase 3)
- 프리셋
- 검색/필터
- Undo/Redo
- 발음 사전

#### 🟢 Low (Phase 4)
- 고급 편집 기능
- 협업 기능
- 외부 통합

---

## Phase 0: 프로젝트 초기 설정

### 📅 기간: 1-2일
### 🎯 목표: 개발 환경 완벽 구축

### ✅ 체크리스트

```
[ ] 1. 프로젝트 디렉토리 생성
[ ] 2. Frontend 프로젝트 초기화
[ ] 3. Backend 프로젝트 초기화
[ ] 4. 데이터베이스 설정
[ ] 5. Git 저장소 설정
[ ] 6. 개발 도구 설치
```

### 📝 상세 작업

#### Step 0.1: 프로젝트 구조 생성

```bash
# 루트 디렉토리 생성
mkdir ssul-maker
cd ssul-maker

# Git 초기화
git init
```

**작업 내용**:
- 프로젝트 루트 디렉토리 생성
- Git 저장소 초기화
- .gitignore 파일 생성

**예상 시간**: 5분

---

#### Step 0.2: Frontend 프로젝트 초기화

```bash
# Vite로 React + TypeScript 프로젝트 생성
npm create vite@latest frontend -- --template react-ts

cd frontend
npm install

# 필수 라이브러리 설치
npm install react-router-dom @reduxjs/toolkit react-redux axios socket.io-client
npm install tailwindcss @radix-ui/react-dialog @radix-ui/react-dropdown-menu
npm install react-hook-form zod @hookform/resolvers
npm install lucide-react dayjs

# 개발 도구
npm install -D @types/node
```

**작업 내용**:
- Vite + React + TypeScript 프로젝트 생성
- 핵심 라이브러리 설치
- Tailwind CSS 설정
- 프로젝트 구조 생성

**예상 시간**: 30분

---

#### Step 0.3: Backend 프로젝트 초기화

```bash
cd ..
mkdir backend
cd backend

# package.json 생성
npm init -y

# 필수 라이브러리 설치
npm install express cors dotenv
npm install better-sqlite3 knex
npm install jsonwebtoken bcryptjs
npm install multer sharp
npm install socket.io
npm install node-cache p-queue
npm install joi express-validator
npm install winston morgan

# 개발 도구
npm install -D nodemon
```

**작업 내용**:
- Node.js 프로젝트 초기화
- 필수 패키지 설치
- 프로젝트 구조 생성
- 환경 변수 설정

**예상 시간**: 30분

---

#### Step 0.4: 데이터베이스 설정

```bash
# 마이그레이션 파일 생성
cd backend
npx knex migrate:make create_users
npx knex migrate:make create_projects
npx knex migrate:make create_panels
npx knex migrate:make create_presets
npx knex migrate:make create_pronunciation_dictionary

# 마이그레이션 실행
npx knex migrate:latest
```

**작업 내용**:
- Knex.js 설정
- 데이터베이스 스키마 마이그레이션 작성
- 초기 마이그레이션 실행
- 시드 데이터 준비 (선택)

**예상 시간**: 1시간

---

#### Step 0.5: Git 및 문서 설정

```bash
# .gitignore 생성
cat > .gitignore << EOF
node_modules/
.env
*.db
dist/
build/
uploads/
logs/
.DS_Store
EOF

# README 생성
cat > README.md << EOF
# SSUL MAKER

스토리텔링 콘텐츠 제작 및 TTS 낭독 플랫폼

## 개발 환경

- Frontend: React 18 + Vite + TypeScript
- Backend: Node.js + Express
- Database: SQLite

## 실행 방법

### Backend
\`\`\`bash
cd backend
npm run dev
\`\`\`

### Frontend
\`\`\`bash
cd frontend
npm run dev
\`\`\`
EOF
```

**작업 내용**:
- .gitignore 설정
- README.md 작성
- 초기 커밋

**예상 시간**: 15분

---

### 🎯 Phase 0 완료 기준

- [x] 프로젝트 구조가 생성됨
- [x] Frontend가 http://localhost:5173 에서 실행됨
- [x] Backend가 http://localhost:3001 에서 실행됨
- [x] 데이터베이스가 생성되고 테이블이 존재함
- [x] Git 저장소가 초기화됨

---

## Phase 1: MVP 핵심 기능

### 📅 기간: 2주
### 🎯 목표: 기본적인 프로젝트 관리 가능

### ✅ 체크리스트

```
Week 1:
[ ] 1.1 인증 시스템 (로그인/회원가입)
[ ] 1.2 사용자 프로필 관리
[ ] 1.3 프로젝트 CRUD API
[ ] 1.4 패널 CRUD API

Week 2:
[ ] 1.5 Frontend 레이아웃 구현
[ ] 1.6 프로젝트 리스트 UI
[ ] 1.7 패널 편집 UI
[ ] 1.8 기본 라우팅 및 네비게이션
```

### 📝 상세 작업

#### Task 1.1: 인증 시스템 구현

**Backend (2일)**

1. **User 모델 및 마이그레이션**
```javascript
// backend/migrations/001_create_users.js
exports.up = function(knex) {
  return knex.schema.createTable('users', table => {
    table.uuid('id').primary();
    table.string('email', 255).notNullable().unique();
    table.string('password', 255).notNullable();
    table.string('name', 100);
    table.string('channel_name', 100);
    table.boolean('email_verified').defaultTo(false);
    table.timestamps(true, true);
  });
};
```

2. **Auth Controller**
```javascript
// backend/src/controllers/authController.js
const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');
const db = require('../config/database');

exports.signup = async (req, res) => {
  try {
    const { email, password, name } = req.body;
    
    // 비밀번호 해싱
    const hashedPassword = await bcrypt.hash(password, 10);
    
    // 사용자 생성
    const [userId] = await db('users').insert({
      id: generateUUID(),
      email,
      password: hashedPassword,
      name,
    });
    
    res.status(201).json({ message: 'User created', userId });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};

exports.login = async (req, res) => {
  try {
    const { email, password } = req.body;
    
    // 사용자 조회
    const user = await db('users').where({ email }).first();
    if (!user) {
      return res.status(401).json({ error: 'Invalid credentials' });
    }
    
    // 비밀번호 확인
    const isValid = await bcrypt.compare(password, user.password);
    if (!isValid) {
      return res.status(401).json({ error: 'Invalid credentials' });
    }
    
    // JWT 생성
    const token = jwt.sign(
      { userId: user.id, email: user.email },
      process.env.JWT_SECRET,
      { expiresIn: '7d' }
    );
    
    res.json({ 
      token, 
      user: { 
        id: user.id, 
        email: user.email, 
        name: user.name 
      } 
    });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
};
```

3. **Auth Middleware**
```javascript
// backend/src/middleware/auth.js
const jwt = require('jsonwebtoken');

module.exports = (req, res, next) => {
  try {
    const token = req.headers.authorization?.split(' ')[1];
    if (!token) {
      return res.status(401).json({ error: 'No token provided' });
    }
    
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded;
    next();
  } catch (error) {
    res.status(401).json({ error: 'Invalid token' });
  }
};
```

4. **Auth Routes**
```javascript
// backend/src/routes/auth.js
const express = require('express');
const router = express.Router();
const authController = require('../controllers/authController');

router.post('/signup', authController.signup);
router.post('/login', authController.login);
router.get('/me', authMiddleware, authController.getMe);

module.exports = router;
```

**Frontend (1일)**

1. **Auth API Client**
```typescript
// frontend/src/api/auth.ts
import { apiClient } from './client';

export const authAPI = {
  signup: (data: { email: string; password: string; name: string }) =>
    apiClient.post('/auth/signup', data),
  
  login: (data: { email: string; password: string }) =>
    apiClient.post('/auth/login', data),
  
  getMe: () =>
    apiClient.get('/auth/me'),
};
```

2. **Auth Redux Slice**
```typescript
// frontend/src/store/slices/authSlice.ts
import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';
import { authAPI } from '@/api/auth';

export const login = createAsyncThunk(
  'auth/login',
  async (credentials: { email: string; password: string }) => {
    const { data } = await authAPI.login(credentials);
    localStorage.setItem('token', data.token);
    return data;
  }
);

const authSlice = createSlice({
  name: 'auth',
  initialState: {
    user: null,
    token: localStorage.getItem('token'),
    isAuthenticated: false,
    loading: false,
    error: null,
  },
  reducers: {
    logout: (state) => {
      state.user = null;
      state.token = null;
      state.isAuthenticated = false;
      localStorage.removeItem('token');
    },
  },
  extraReducers: (builder) => {
    builder
      .addCase(login.pending, (state) => {
        state.loading = true;
      })
      .addCase(login.fulfilled, (state, action) => {
        state.loading = false;
        state.isAuthenticated = true;
        state.user = action.payload.user;
        state.token = action.payload.token;
      })
      .addCase(login.rejected, (state, action) => {
        state.loading = false;
        state.error = action.error.message;
      });
  },
});

export const { logout } = authSlice.actions;
export default authSlice.reducer;
```

3. **Login Page**
```typescript
// frontend/src/pages/Login.tsx
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { useAppDispatch } from '@/store/hooks';
import { login } from '@/store/slices/authSlice';

const loginSchema = z.object({
  email: z.string().email('이메일 형식이 올바르지 않습니다'),
  password: z.string().min(6, '비밀번호는 최소 6자 이상입니다'),
});

export default function Login() {
  const dispatch = useAppDispatch();
  const { register, handleSubmit, formState: { errors } } = useForm({
    resolver: zodResolver(loginSchema),
  });

  const onSubmit = async (data: any) => {
    await dispatch(login(data));
  };

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-100">
      <div className="bg-white p-8 rounded-lg shadow-md w-96">
        <h1 className="text-2xl font-bold mb-6">SSUL MAKER 로그인</h1>
        <form onSubmit={handleSubmit(onSubmit)}>
          <div className="mb-4">
            <label className="block text-sm font-medium mb-2">이메일</label>
            <input
              {...register('email')}
              type="email"
              className="w-full px-3 py-2 border rounded-lg"
            />
            {errors.email && (
              <p className="text-red-500 text-sm mt-1">{errors.email.message}</p>
            )}
          </div>
          
          <div className="mb-6">
            <label className="block text-sm font-medium mb-2">비밀번호</label>
            <input
              {...register('password')}
              type="password"
              className="w-full px-3 py-2 border rounded-lg"
            />
            {errors.password && (
              <p className="text-red-500 text-sm mt-1">{errors.password.message}</p>
            )}
          </div>
          
          <button
            type="submit"
            className="w-full bg-red-600 text-white py-2 rounded-lg hover:bg-red-700"
          >
            로그인
          </button>
        </form>
      </div>
    </div>
  );
}
```

**예상 시간**: 3일

---

#### Task 1.2: 프로젝트 CRUD 구현

**Backend (1일)**

1. **Project 모델**
2. **Project Controller**
3. **Project Routes**

**Frontend (1일)**

1. **Project API Client**
2. **Project Redux Slice**
3. **Project List Component**

**예상 시간**: 2일

---

#### Task 1.3: 패널 CRUD 구현

**Backend (1일)**
**Frontend (1일)**

**예상 시간**: 2일

---

#### Task 1.4: 기본 레이아웃 구현

**Frontend (2일)**

1. **3-Column Layout**
2. **Sidebar Component**
3. **CenterPanel Component**
4. **RightPanel Component**

**예상 시간**: 2일

---

### 🎯 Phase 1 완료 기준

- [x] 사용자가 회원가입/로그인할 수 있음
- [x] 프로젝트를 생성/조회/수정/삭제할 수 있음
- [x] 패널을 추가/편집/삭제할 수 있음
- [x] 기본 UI가 구현되어 있음
- [x] 데이터가 DB에 저장됨

---

## Phase 2: 핵심 기능 고도화

### 📅 기간: 3주
### 🎯 목표: TTS 및 파일 처리 기능 완성

### ✅ 체크리스트

```
Week 1:
[ ] 2.1 파일 업로드 시스템
[ ] 2.2 이미지 처리 (Sharp)
[ ] 2.3 Gemini TTS 통합

Week 2:
[ ] 2.4 오디오 플레이어 구현
[ ] 2.5 TTS 진행률 표시
[ ] 2.6 작업 큐 시스템

Week 3:
[ ] 2.7 자동 저장 기능
[ ] 2.8 WebSocket 실시간 업데이트
[ ] 2.9 에러 처리 개선
```

---

## Phase 3: 고급 기능 추가

### 📅 기간: 4주
### 🎯 목표: 사용자 경험 향상

### ✅ 체크리스트

```
Week 1:
[ ] 3.1 프리셋 시스템
[ ] 3.2 검색 및 필터
[ ] 3.3 브레드크럼 네비게이션

Week 2:
[ ] 3.4 Undo/Redo 기능
[ ] 3.5 패널 복사/붙여넣기
[ ] 3.6 일괄 편집

Week 3:
[ ] 3.7 발음 사전
[ ] 3.8 TTS 비용 관리
[ ] 3.9 사용자 설정

Week 4:
[ ] 3.10 오프라인 지원 (기본)
[ ] 3.11 모바일 UX 개선
[ ] 3.12 접근성 강화
```

---

## Phase 4: 최적화 및 배포

### 📅 기간: 2주
### 🎯 목표: Production 준비

### ✅ 체크리스트

```
Week 1:
[ ] 4.1 성능 최적화
[ ] 4.2 테스트 작성
[ ] 4.3 보안 강화

Week 2:
[ ] 4.4 배포 설정
[ ] 4.5 문서화
[ ] 4.6 사용자 가이드
```

---

## 각 Phase별 상세 작업

### Phase 1 상세 작업 목록

#### Week 1

**Day 1-2: 인증 시스템**
- [ ] User 테이블 마이그레이션
- [ ] 회원가입 API
- [ ] 로그인 API
- [ ] JWT 미들웨어
- [ ] 로그인 페이지 UI
- [ ] 회원가입 페이지 UI

**Day 3-4: 프로젝트 CRUD**
- [ ] Project 테이블 마이그레이션
- [ ] 프로젝트 생성 API
- [ ] 프로젝트 조회 API
- [ ] 프로젝트 수정 API
- [ ] 프로젝트 삭제 API
- [ ] 프로젝트 리스트 UI

**Day 5: 패널 CRUD**
- [ ] Panel 테이블 마이그레이션
- [ ] 패널 CRUD API
- [ ] 패널 순서 변경 API

#### Week 2

**Day 1-2: 기본 레이아웃**
- [ ] 3-Column Layout 구현
- [ ] Sidebar Component
- [ ] 좌측 프로젝트 리스트
- [ ] 우측 패널 설정

**Day 3-4: 패널 편집 UI**
- [ ] 패널 리스트 컴포넌트
- [ ] 패널 아이템 컴포넌트
- [ ] 스크립트 입력 필드
- [ ] 패널 추가/삭제 버튼

**Day 5: 통합 및 테스트**
- [ ] API 연동 테스트
- [ ] 버그 수정
- [ ] Phase 1 완료 확인

---

### Phase 2 상세 작업 목록

#### Week 1

**Day 1-2: 파일 업로드**
- [ ] Multer 설정
- [ ] 이미지 업로드 API
- [ ] Sharp 이미지 처리
- [ ] WebP 변환
- [ ] 이미지 압축

**Day 3-5: Gemini TTS**
- [ ] Gemini API 설정
- [ ] TTS 생성 API
- [ ] 오디오 파일 저장
- [ ] 작업 큐 통합

#### Week 2

**Day 1-2: 오디오 플레이어**
- [ ] WaveSurfer.js 통합
- [ ] 재생/일시정지
- [ ] 볼륨 조절
- [ ] 타임라인 표시

**Day 3-4: TTS 진행률**
- [ ] 진행률 모달 UI
- [ ] WebSocket 이벤트
- [ ] 실시간 업데이트

**Day 5: 작업 큐**
- [ ] p-queue 설정
- [ ] 동시성 제어
- [ ] 큐 상태 모니터링

#### Week 3

**Day 1-2: 자동 저장**
- [ ] Debounce 구현
- [ ] 저장 상태 표시
- [ ] 충돌 해결

**Day 3-4: WebSocket**
- [ ] Socket.io 설정
- [ ] 실시간 알림
- [ ] 연결 관리

**Day 5: 에러 처리**
- [ ] 에러 핸들러
- [ ] 토스트 알림
- [ ] 로깅 시스템

---

## 🎯 전체 마일스톤

```
Week 1-2   : Phase 0 + Phase 1 시작
Week 3-4   : Phase 1 완료
Week 5-7   : Phase 2 완료
Week 8-11  : Phase 3 완료
Week 12-13 : Phase 4 완료
```

### 예상 총 기간: **약 3개월**

---

## 📊 진행 상황 추적

### Phase 완료 체크리스트

```
[ ] Phase 0: 프로젝트 초기 설정 (완료일: ____)
[ ] Phase 1: MVP 핵심 기능 (완료일: ____)
[ ] Phase 2: 핵심 기능 고도화 (완료일: ____)
[ ] Phase 3: 고급 기능 추가 (완료일: ____)
[ ] Phase 4: 최적화 및 배포 (완료일: ____)
```

---

## 🚦 다음 단계

### 지금 바로 시작하기

1. **Phase 0.1 부터 시작**
```bash
mkdir ssul-maker
cd ssul-maker
git init
```

2. **Frontend 프로젝트 생성**
```bash
npm create vite@latest frontend -- --template react-ts
```

3. **Backend 프로젝트 생성**
```bash
mkdir backend
cd backend
npm init -y
```

4. **첫 번째 커밋**
```bash
git add .
git commit -m "Initial commit: Project structure"
```

---

## 🎬 2026-05-04 개정: 영상 편집기 중심 구현 로드맵

기존 로드맵은 프로젝트/패널 CRUD와 인증을 중심으로 구성되어 있습니다. 앞으로의 구현은 "썰 영상 제작용 편집기"에 필요한 제작 흐름을 기준으로 재정렬합니다.

### 개정된 MVP 목표

사용자가 하나의 프로젝트에서 대본을 씬 단위로 작성하고, 각 씬에 이미지와 TTS 음성을 붙인 뒤, 9:16 프리뷰와 타임라인으로 확인하고, 최종 mp4를 export할 수 있게 합니다.

### Phase A: 현재 MVP 안정화

기간: 2-3일

- [ ] 깨진 한글 UI 문구 정리
- [ ] 프론트 lint 오류 수정
- [ ] `better-sqlite3` Node 버전 mismatch 해결
- [ ] 패널 reorder API 라우트 순서 수정
- [ ] README/실제 package 버전 불일치 정리
- [ ] `work-log.md`에 작업 기록 시작

완료 기준:
- `frontend npm run build` 성공
- `frontend npm run lint` 성공
- `backend npm run migrate` 성공
- 프로젝트 생성, 패널 생성, 패널 정렬이 정상 동작

### Phase B: 영상 편집 작업대 UI

기간: 4-5일

- [ ] `EditorToolbar` 추가: 저장 상태, TTS 생성, 미리보기, export 버튼
- [ ] `VideoPreviewCanvas` 추가: 9:16 프리뷰, 자막, 배경, 이미지 표시
- [ ] `SceneTimeline` 추가: 씬 순서, 오디오 길이, playhead 표시
- [ ] `SceneInspector`로 우측 설정 패널 재구성
- [ ] 패널 용어를 UI에서는 "씬"으로 정리

완료 기준:
- 사용자가 선택한 씬이 중앙 프리뷰에 즉시 반영
- 씬 순서 변경이 타임라인과 DB에 반영
- 전체 영상 길이 표시

### Phase C: 이미지 업로드와 씬 연결

기간: 3-4일

- [ ] `POST /api/scenes/:sceneId/image` 또는 기존 panel 기반 업로드 API 구현
- [ ] Multer로 이미지 업로드
- [ ] Sharp로 WebP 변환, 리사이즈, 썸네일 생성
- [ ] 이미지 메타데이터 저장: width, height, file size, format
- [ ] 프론트 이미지 업로드/교체/삭제 UX 구현
- [ ] 프리뷰 캔버스에 이미지 fit/cover 표시

완료 기준:
- 씬별 이미지가 업로드되고 프리뷰에 표시
- 업로드 실패 시 복구 가능한 에러 표시

### Phase D: TTS 생성

기간: 4-6일

- [ ] `GEMINI_API_KEY`, `GEMINI_TTS_MODEL`, `TTS_DEFAULT_VOICE` 환경변수 정의
- [ ] `ttsService.generateSpeech()` 구현
- [ ] `script + voice + speed + instructions + model` hash 캐시 구현
- [ ] `POST /api/scenes/:sceneId/tts` 구현
- [ ] 생성된 mp3를 `uploads/audio`에 저장
- [ ] 오디오 duration 계산
- [ ] 씬에 `audio_url`, `audio_duration`, `tts_status` 저장
- [ ] 프론트 오디오 플레이어와 TTS 상태 표시 구현

완료 기준:
- 씬 단위 TTS 생성/재생 가능
- 같은 입력의 TTS 재생성 시 기존 오디오 재사용
- 실패 시 재시도 가능

### Phase E: 전체 재생과 타임라인

기간: 4-5일

- [ ] 씬별 duration을 기준으로 전체 재생 시간 계산
- [ ] 전체 재생 버튼 구현
- [ ] playhead와 현재 씬 동기화
- [ ] 씬 클릭 시 해당 시점으로 이동
- [ ] TTS 없는 씬은 기본 duration 또는 경고 상태 표시

완료 기준:
- 사용자가 전체 영상을 export 전에 순서대로 확인 가능
- playhead 이동에 따라 프리뷰 씬이 변경

### Phase F: 첫 영상 export

기간: 5-7일

- [ ] FFmpeg 실행 환경 확인
- [ ] 렌더 입력 JSON 생성
- [ ] 이미지 + 오디오 + 자막으로 씬별 클립 생성
- [ ] 씬 클립 concat
- [ ] mp4 결과물을 `uploads/renders`에 저장
- [ ] `render_jobs` 상태 API 구현
- [ ] 프론트 export 진행률/완료/실패 UX 구현

완료 기준:
- 9:16 mp4 파일이 생성되고 다운로드 가능
- 렌더 실패 사유가 사용자에게 표시

### Phase G: 제작 속도 개선

기간: 1-2주

- [ ] 전체 스크립트 붙여넣기 후 자동 씬 분할
- [ ] 전체 TTS 일괄 생성
- [ ] 자막 스타일 프리셋
- [ ] 프로젝트 템플릿
- [ ] Undo/Redo
- [ ] 자동 저장 디바운스

### 기능별 구현 전략 요약

| 기능 | 1차 구현 | 이후 확장 |
|------|----------|-----------|
| TTS | Gemini TTS, PCM 수신 후 mp3 변환 저장, polling | Socket.io 진행률, 일괄 생성, 비용/사용량 |
| 이미지 | 로컬 업로드, Sharp 최적화 | S3/CDN, 크롭 UI, AI 이미지 생성 |
| 프리뷰 | React DOM 기반 9:16 프리뷰 | Canvas 렌더, safe area, transition preview |
| 타임라인 | 씬 카드 + duration + playhead | waveform, clip trim, transition 편집 |
| Export | FFmpeg 로컬 mp4 생성 | 큐 기반 병렬 렌더, 클라우드 렌더 |
| 저장 | 명시적 저장 + 상태 표시 | 자동 저장, 충돌 복구, 버전 관리 |

---

## 📝 참고 문서

- [요구사항서.md](./요구사항서.md)
- [설계서.md](./설계서.md)
- [UX_UI_기능설명서.md](./UX_UI_기능설명서.md)
- [기술스택_명세서.md](./기술스택_명세서.md)
- [로컬개발환경_설정가이드.md](./로컬개발환경_설정가이드.md)
- [개선_필요사항_분석.md](./개선_필요사항_분석.md)

---

**이제 Phase 0 부터 시작하시겠습니까?** 🚀


