# 🛠️ SSUL MAKER - 기술 스택 명세서

## 📌 문서 정보
- **프로젝트명**: SSUL MAKER (썰 메이커)
- **환경**: 로컬 PC 개발 환경
- **작성일**: 2025-10-06
- **문서 유형**: 기술 스택 명세서

---

## 목차
1. [기술 스택 개요](#1-기술-스택-개요)
2. [프론트엔드 (Frontend)](#2-프론트엔드-frontend)
3. [백엔드 (Backend)](#3-백엔드-backend)
4. [데이터베이스 (Database)](#4-데이터베이스-database)
5. [캐시 시스템](#5-캐시-시스템)
6. [작업 큐](#6-작업-큐)
7. [개발 도구](#7-개발-도구)
8. [배포 및 확장](#8-배포-및-확장)

---

## 1. 기술 스택 개요

### 1.1 선택된 기술 스택

```
┌─────────────────────────────────────────────────────────┐
│                    SSUL MAKER 기술 스택                  │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Frontend:  React 18 + Vite + TypeScript                │
│  Backend:   Node.js + Express                           │
│  Database:  SQLite (better-sqlite3)                     │
│  Cache:     node-cache (메모리 캐시)                    │
│  Queue:     p-queue (간단한 큐)                         │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 1.2 선택 이유

#### ✅ 로컬 개발 최적화
- 설치 및 설정 간소화
- 리소스 사용량 최소화
- 빠른 개발 속도

#### ✅ 학습 곡선 완만
- 널리 사용되는 기술
- 풍부한 문서와 커뮤니티
- 쉬운 트러블슈팅

#### ✅ 확장 가능
- Production 환경으로 쉽게 전환
- 필요시 더 강력한 도구로 업그레이드 가능
- 코드 변경 최소화

---

## 2. 프론트엔드 (Frontend)

### 2.1 핵심 기술

#### React 18.2.0
```
- UI 라이브러리
- Component 기반 구조
- Virtual DOM으로 성능 최적화
- 풍부한 생태계
```

**선택 이유**:
- ✅ 가장 인기있는 프론트엔드 라이브러리
- ✅ 풍부한 서드파티 라이브러리
- ✅ React Hooks로 상태 관리 간편
- ✅ 뛰어난 개발자 경험

#### Vite 5.0
```
- 빌드 도구
- 빠른 HMR (Hot Module Replacement)
- ESBuild 기반 번들링
```

**선택 이유**:
- ✅ Create React App보다 10배 빠름
- ✅ 즉각적인 서버 시작
- ✅ 빠른 리로드 속도
- ✅ 프로덕션 최적화 자동

#### TypeScript 5.3.0
```
- 정적 타입 시스템
- JavaScript의 슈퍼셋
- 컴파일 시점 에러 감지
```

**선택 이유**:
- ✅ 타입 안정성
- ✅ IDE 자동완성 지원
- ✅ 리팩토링 용이
- ✅ 버그 사전 방지

### 2.2 주요 라이브러리

#### 상태 관리
```json
{
  "@reduxjs/toolkit": "^2.0.0",
  "react-redux": "^9.0.0",
  "zustand": "^4.4.0"
}
```

**Redux Toolkit**: 전역 상태 관리
- 프로젝트, 패널, 사용자 상태
- 복잡한 상태 로직
- DevTools 통합

**Zustand**: 간단한 상태 관리
- UI 상태 (모달, 사이드바)
- 가벼운 상태
- 보일러플레이트 최소화

#### UI 라이브러리
```json
{
  "tailwindcss": "^3.4.0",
  "@radix-ui/react-dialog": "^1.0.5",
  "@radix-ui/react-dropdown-menu": "^2.0.6",
  "@radix-ui/react-select": "^2.0.0",
  "lucide-react": "^0.300.0"
}
```

**Tailwind CSS**: 유틸리티 기반 CSS
- 빠른 스타일링
- 일관된 디자인 시스템
- 작은 번들 크기

**Radix UI**: 접근성 우선 컴포넌트
- WAI-ARIA 준수
- 키보드 네비게이션
- 커스터마이징 용이

**Lucide React**: 아이콘 라이브러리
- 1000+ 아이콘
- 트리셰이킹 지원
- TypeScript 타입 포함

#### 폼 관리
```json
{
  "react-hook-form": "^7.49.0",
  "zod": "^3.22.4",
  "@hookform/resolvers": "^3.3.4"
}
```

**React Hook Form**: 성능 최적화 폼
- 리렌더링 최소화
- 간단한 API
- 유효성 검사 통합

**Zod**: 스키마 기반 유효성 검사
- TypeScript 우선
- 타입 추론
- 명확한 에러 메시지

#### 오디오 처리
```json
{
  "wavesurfer.js": "^7.6.0",
  "react-wavesurfer": "^3.0.0"
}
```

**WaveSurfer.js**: 오디오 시각화
- 파형 표시
- 재생 제어
- 커스터마이징 가능

#### 가상 스크롤
```json
{
  "react-virtuoso": "^4.6.2"
}
```

**React Virtuoso**: 100개+ 패널 처리
- 부드러운 스크롤
- 자동 높이 계산
- 메모리 효율적

#### 드래그 앤 드롭
```json
{
  "@dnd-kit/core": "^6.1.0",
  "@dnd-kit/sortable": "^8.0.0"
}
```

**DnD Kit**: 패널 순서 변경
- 터치 지원
- 접근성 우선
- 커스터마이징 용이

#### HTTP 클라이언트
```json
{
  "axios": "^1.6.2",
  "socket.io-client": "^4.6.1"
}
```

**Axios**: REST API 통신
- Interceptors 지원
- 자동 JSON 변환
- 타임아웃 설정

**Socket.io Client**: 실시간 통신
- WebSocket 기반
- 자동 재연결
- 이벤트 기반

#### 오프라인 지원
```json
{
  "workbox-webpack-plugin": "^7.0.0",
  "dexie": "^3.2.4"
}
```

**Workbox**: Service Worker
- 오프라인 캐싱
- 백그라운드 동기화
- PWA 지원

**Dexie**: IndexedDB 래퍼
- Promise 기반 API
- 쿼리 지원
- 트랜잭션 관리

### 2.3 프로젝트 구조

```
frontend/
├── public/
│   ├── favicon.ico
│   └── manifest.json
├── src/
│   ├── components/          # UI 컴포넌트
│   │   ├── layout/
│   │   │   ├── Sidebar.tsx
│   │   │   ├── CenterPanel.tsx
│   │   │   └── RightPanel.tsx
│   │   ├── project/
│   │   │   ├── ProjectList.tsx
│   │   │   ├── ProjectCard.tsx
│   │   │   └── ProjectDetail.tsx
│   │   ├── panel/
│   │   │   ├── PanelList.tsx
│   │   │   ├── PanelItem.tsx
│   │   │   ├── PanelEditor.tsx
│   │   │   └── PanelSettings.tsx
│   │   ├── audio/
│   │   │   ├── AudioPlayer.tsx
│   │   │   ├── AudioWaveform.tsx
│   │   │   └── AudioControls.tsx
│   │   ├── tts/
│   │   │   ├── TTSProgressModal.tsx
│   │   │   ├── VoiceSelector.tsx
│   │   │   └── TTSUsageWidget.tsx
│   │   └── common/
│   │       ├── Button.tsx
│   │       ├── Input.tsx
│   │       ├── Modal.tsx
│   │       ├── Toast.tsx
│   │       └── Loading.tsx
│   ├── pages/               # 페이지
│   │   ├── Dashboard.tsx
│   │   ├── Login.tsx
│   │   ├── Signup.tsx
│   │   └── Settings.tsx
│   ├── hooks/               # Custom Hooks
│   │   ├── useAuth.ts
│   │   ├── useProject.ts
│   │   ├── usePanel.ts
│   │   ├── useAudio.ts
│   │   └── useWebSocket.ts
│   ├── store/               # Redux Store
│   │   ├── slices/
│   │   │   ├── authSlice.ts
│   │   │   ├── projectSlice.ts
│   │   │   ├── panelSlice.ts
│   │   │   └── uiSlice.ts
│   │   └── store.ts
│   ├── api/                 # API 클라이언트
│   │   ├── client.ts
│   │   ├── auth.ts
│   │   ├── project.ts
│   │   ├── panel.ts
│   │   └── tts.ts
│   ├── utils/               # 유틸리티
│   │   ├── formatters.ts
│   │   ├── validators.ts
│   │   └── constants.ts
│   ├── types/               # TypeScript 타입
│   │   ├── index.ts
│   │   ├── api.ts
│   │   └── models.ts
│   ├── styles/              # 전역 스타일
│   │   └── globals.css
│   ├── App.tsx
│   ├── main.tsx
│   └── vite-env.d.ts
├── .env
├── .env.example
├── .gitignore
├── index.html
├── package.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
└── tailwind.config.js
```

### 2.4 설정 파일

#### vite.config.ts
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
      '@components': path.resolve(__dirname, './src/components'),
      '@pages': path.resolve(__dirname, './src/pages'),
      '@hooks': path.resolve(__dirname, './src/hooks'),
      '@store': path.resolve(__dirname, './src/store'),
      '@api': path.resolve(__dirname, './src/api'),
      '@utils': path.resolve(__dirname, './src/utils'),
      '@types': path.resolve(__dirname, './src/types'),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3001',
        changeOrigin: true,
      },
      '/socket.io': {
        target: 'http://localhost:3001',
        ws: true,
      },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom', 'react-router-dom'],
          redux: ['@reduxjs/toolkit', 'react-redux'],
          ui: ['@radix-ui/react-dialog', '@radix-ui/react-dropdown-menu'],
        },
      },
    },
  },
});
```

#### tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "paths": {
      "@/*": ["./src/*"],
      "@components/*": ["./src/components/*"],
      "@pages/*": ["./src/pages/*"],
      "@hooks/*": ["./src/hooks/*"],
      "@store/*": ["./src/store/*"],
      "@api/*": ["./src/api/*"],
      "@utils/*": ["./src/utils/*"],
      "@types/*": ["./src/types/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

#### .env.example
```env
# API URL
VITE_API_URL=http://localhost:3001/api

# WebSocket URL
VITE_WS_URL=http://localhost:3001

# AI API keys are backend-only.

# Environment
VITE_ENV=development
```

---

## 3. 백엔드 (Backend)

### 3.1 핵심 기술

#### Node.js 20.10.0 LTS
```
- JavaScript 런타임
- V8 엔진 기반
- 비동기 I/O
- 이벤트 기반
```

**선택 이유**:
- ✅ JavaScript로 Full-stack 개발
- ✅ npm 생태계
- ✅ 높은 성능
- ✅ 활발한 커뮤니티

#### Express.js 4.18
```
- 웹 프레임워크
- 미들웨어 기반
- 간결한 API
```

**선택 이유**:
- ✅ 가장 인기있는 Node.js 프레임워크
- ✅ 유연한 구조
- ✅ 풍부한 미들웨어
- ✅ 학습 용이

### 3.2 주요 라이브러리

#### 데이터베이스
```json
{
  "better-sqlite3": "^9.2.2",
  "knex": "^3.1.0"
}
```

**better-sqlite3**: SQLite 드라이버
- 동기식 API
- 빠른 성능
- TypeScript 지원

**Knex.js**: SQL 쿼리 빌더
- 마이그레이션 지원
- 트랜잭션 관리
- 다중 DB 지원 (나중에 PostgreSQL 전환 쉬움)

#### 인증 및 보안
```json
{
  "jsonwebtoken": "^9.0.2",
  "bcryptjs": "^2.4.3",
  "passport": "^0.7.0",
  "passport-jwt": "^4.0.1",
  "speakeasy": "^2.0.0"
}
```

**JWT**: 토큰 기반 인증
- Stateless 인증
- 확장 가능
- 모바일 친화적

**bcryptjs**: 비밀번호 암호화
- 강력한 해싱
- Salt 자동 생성
- 순수 JavaScript

**Speakeasy**: 2FA (Two-Factor Authentication)
- TOTP 생성
- QR 코드
- 백업 코드

#### 파일 처리
```json
{
  "multer": "^1.4.5-lts.1",
  "sharp": "^0.33.1",
  "fluent-ffmpeg": "^2.1.2"
}
```

**Multer**: 파일 업로드
- Multipart 폼 데이터
- 메모리/디스크 저장
- 파일 필터링

**Sharp**: 이미지 처리
- 리사이징, 크롭
- WebP 변환
- 압축
- 빠른 성능

**FFmpeg**: 오디오 처리
- 포맷 변환
- 병합, 자르기
- 페이드 효과
- 볼륨 조절

#### WebSocket
```json
{
  "socket.io": "^4.6.1"
}
```

**Socket.io**: 실시간 통신
- 양방향 통신
- 자동 재연결
- 룸/네임스페이스
- 이벤트 기반

#### 캐시 (메모리)
```json
{
  "node-cache": "^5.1.2"
}
```

**node-cache**: 간단한 캐싱
- 인메모리 저장
- TTL 지원
- 간단한 API
- Redis 없이 사용 가능

#### 작업 큐
```json
{
  "p-queue": "^8.0.1"
}
```

**p-queue**: Promise 기반 큐
- 동시성 제어
- 우선순위
- 간단한 API
- Redis 불필요

#### 유효성 검사
```json
{
  "joi": "^17.11.0",
  "express-validator": "^7.0.1"
}
```

**Joi**: 스키마 기반 검증
- 강력한 유효성 검사
- 명확한 에러 메시지
- TypeScript 지원

**express-validator**: Express 미들웨어
- 요청 검증
- Sanitization
- 커스텀 검증

#### 로깅
```json
{
  "winston": "^3.11.0",
  "morgan": "^1.10.0"
}
```

**Winston**: 애플리케이션 로깅
- 다중 출력
- 로그 레벨
- 포맷 커스터마이징

**Morgan**: HTTP 요청 로깅
- 요청/응답 로깅
- 다양한 포맷
- Express 통합

#### Gemini TTS

**Gemini API REST 호출**: TTS 생성
- `gemini-2.5-flash-preview-tts`
- 24kHz PCM 응답 수신 후 FFmpeg로 mp3 변환
- API 키는 백엔드 환경변수로만 관리

#### 유틸리티
```json
{
  "lodash": "^4.17.21",
  "dayjs": "^1.11.10",
  "dotenv": "^16.3.1",
  "cors": "^2.8.5"
}
```

### 3.3 프로젝트 구조

```
backend/
├── src/
│   ├── config/              # 설정
│   │   ├── database.js
│   │   ├── jwt.js
│   │   ├── gemini.js
│   │   └── upload.js
│   ├── middleware/          # 미들웨어
│   │   ├── auth.js
│   │   ├── errorHandler.js
│   │   ├── validation.js
│   │   ├── upload.js
│   │   └── rateLimiter.js
│   ├── models/              # 모델 (Knex 쿼리)
│   │   ├── User.js
│   │   ├── Project.js
│   │   ├── Panel.js
│   │   └── PronunciationDictionary.js
│   ├── routes/              # 라우트
│   │   ├── index.js
│   │   ├── auth.js
│   │   ├── projects.js
│   │   ├── panels.js
│   │   ├── tts.js
│   │   ├── presets.js
│   │   └── uploads.js
│   ├── controllers/         # 컨트롤러
│   │   ├── authController.js
│   │   ├── projectController.js
│   │   ├── panelController.js
│   │   ├── ttsController.js
│   │   └── presetController.js
│   ├── services/            # 비즈니스 로직
│   │   ├── authService.js
│   │   ├── ttsService.js
│   │   ├── audioService.js
│   │   ├── imageService.js
│   │   ├── queueService.js
│   │   └── cacheService.js
│   ├── validators/          # 유효성 검사
│   │   ├── authValidator.js
│   │   ├── projectValidator.js
│   │   └── panelValidator.js
│   ├── utils/               # 유틸리티
│   │   ├── logger.js
│   │   ├── errors.js
│   │   └── helpers.js
│   ├── socket/              # WebSocket
│   │   ├── index.js
│   │   └── handlers.js
│   └── app.js               # Express 앱
├── migrations/              # DB 마이그레이션
│   ├── 001_create_users.js
│   ├── 002_create_projects.js
│   ├── 003_create_panels.js
│   ├── 004_create_presets.js
│   └── 005_create_pronunciation_dictionary.js
├── seeds/                   # 시드 데이터
│   ├── 001_users.js
│   └── 002_presets.js
├── uploads/                 # 업로드 파일
│   ├── images/
│   └── audio/
├── data/                    # SQLite DB
│   └── ssulmaker.db
├── tests/                   # 테스트
│   ├── unit/
│   └── integration/
├── .env
├── .env.example
├── .gitignore
├── package.json
├── knexfile.js
└── server.js                # 서버 시작점
```

### 3.4 설정 파일

#### knexfile.js
```javascript
require('dotenv').config();
const path = require('path');

module.exports = {
  development: {
    client: 'better-sqlite3',
    connection: {
      filename: path.resolve(__dirname, 'data/ssulmaker.db')
    },
    useNullAsDefault: true,
    migrations: {
      directory: './migrations',
      tableName: 'knex_migrations',
    },
    seeds: {
      directory: './seeds',
    },
  },
  
  // 나중에 PostgreSQL로 전환 시
  production: {
    client: 'postgresql',
    connection: {
      host: process.env.DB_HOST,
      port: process.env.DB_PORT || 5432,
      database: process.env.DB_NAME,
      user: process.env.DB_USER,
      password: process.env.DB_PASSWORD,
    },
    pool: {
      min: 2,
      max: 10
    },
    migrations: {
      directory: './migrations',
      tableName: 'knex_migrations',
    },
  }
};
```

#### .env.example
```env
# Server
NODE_ENV=development
PORT=3001
HOST=localhost

# Database
DATABASE_PATH=./data/ssulmaker.db

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your-refresh-token-secret
JWT_REFRESH_EXPIRES_IN=30d

# Gemini
GEMINI_API_KEY=your-gemini-api-key
GEMINI_TTS_MODEL=gemini-2.5-flash-preview-tts

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
ALLOWED_IMAGE_TYPES=image/jpeg,image/png,image/gif,image/webp

# CORS
FRONTEND_URL=http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Cache
CACHE_TTL=3600

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=info
LOG_DIR=./logs
```

#### package.json scripts
```json
{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "knex migrate:latest",
    "migrate:rollback": "knex migrate:rollback",
    "migrate:make": "knex migrate:make",
    "seed": "knex seed:run",
    "db:reset": "knex migrate:rollback && knex migrate:latest && knex seed:run",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint .",
    "format": "prettier --write \"src/**/*.js\""
  }
}
```

---

## 4. 데이터베이스 (Database)

### 4.1 SQLite

#### 선택 이유
```
✅ Zero Configuration - 설치 불필요
✅ 파일 기반 - 단일 파일로 관리
✅ 빠른 성능 - 로컬 I/O 속도
✅ 가벼움 - 메모리 사용량 최소
✅ 백업 간단 - 파일 복사만
✅ 개발 단계에 완벽
```

#### better-sqlite3 vs sqlite3
```javascript
// better-sqlite3 (추천!)
const db = require('better-sqlite3')('data/ssulmaker.db');
const row = db.prepare('SELECT * FROM users WHERE id = ?').get(userId);
// 동기식, 빠름, TypeScript 지원

// sqlite3
const db = require('sqlite3').Database('data/ssulmaker.db');
db.get('SELECT * FROM users WHERE id = ?', [userId], (err, row) => {
  // 비동기식, 콜백 지옥
});
```

**better-sqlite3 선택 이유**:
- ✅ 3-5배 빠른 성능
- ✅ 동기식 API (코드 간결)
- ✅ TypeScript 지원
- ✅ Prepared statements

### 4.2 데이터베이스 스키마

#### Users 테이블
```sql
CREATE TABLE users (
  id TEXT PRIMARY KEY,
  email TEXT NOT NULL UNIQUE,
  password TEXT NOT NULL,
  name TEXT,
  channel_name TEXT,
  profile_image_url TEXT,
  email_verified INTEGER DEFAULT 0,
  two_factor_secret TEXT,
  two_factor_enabled INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  deleted_at DATETIME
);

CREATE INDEX idx_users_email ON users(email);
```

#### Projects 테이블
```sql
CREATE TABLE projects (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  title TEXT NOT NULL,
  description TEXT,
  channel_name TEXT,
  view_count INTEGER DEFAULT 0,
  show_author INTEGER DEFAULT 1,
  status TEXT DEFAULT 'draft',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  deleted_at DATETIME,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE INDEX idx_projects_user ON projects(user_id);
CREATE INDEX idx_projects_status ON projects(status);
```

#### Panels 테이블
```sql
CREATE TABLE panels (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  order_index INTEGER NOT NULL,
  script TEXT NOT NULL,
  image_url TEXT,
  audio_url TEXT,
  voice_id TEXT DEFAULT 'Jiny',
  voice_speed REAL DEFAULT 1.0,
  text_size INTEGER DEFAULT 22,
  text_color TEXT DEFAULT '#FFFFFF',
  background_color TEXT DEFAULT '#000000',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);

CREATE INDEX idx_panels_project ON panels(project_id);
CREATE INDEX idx_panels_order ON panels(project_id, order_index);
```

#### Presets 테이블
```sql
CREATE TABLE presets (
  id TEXT PRIMARY KEY,
  user_id TEXT,
  name TEXT NOT NULL,
  description TEXT,
  thumbnail_url TEXT,
  is_public INTEGER DEFAULT 0,
  usage_count INTEGER DEFAULT 0,
  data TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL
);

CREATE INDEX idx_presets_public ON presets(is_public);
CREATE INDEX idx_presets_usage ON presets(usage_count DESC);
```

#### Pronunciation Dictionary 테이블
```sql
CREATE TABLE pronunciation_dictionary (
  id TEXT PRIMARY KEY,
  user_id TEXT,
  word TEXT NOT NULL,
  pronunciation TEXT NOT NULL,
  phonetic_symbol TEXT,
  audio_sample_url TEXT,
  is_global INTEGER DEFAULT 0,
  usage_count INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE INDEX idx_pronunciation_word ON pronunciation_dictionary(word);
CREATE INDEX idx_pronunciation_user ON pronunciation_dictionary(user_id);
```

### 4.3 Knex.js 사용 예시

#### 쿼리 빌더
```javascript
const db = require('./config/database');

// SELECT
const users = await db('users')
  .where({ email: 'user@example.com' })
  .first();

// INSERT
const [userId] = await db('users').insert({
  id: generateId(),
  email: 'new@example.com',
  password: hashedPassword,
  name: 'John Doe',
});

// UPDATE
await db('users')
  .where({ id: userId })
  .update({
    name: 'Jane Doe',
    updated_at: db.fn.now(),
  });

// DELETE (Soft Delete)
await db('users')
  .where({ id: userId })
  .update({ deleted_at: db.fn.now() });

// JOIN
const projects = await db('projects')
  .join('users', 'projects.user_id', 'users.id')
  .select('projects.*', 'users.name as author_name')
  .where({ 'projects.user_id': userId });

// Transaction
await db.transaction(async (trx) => {
  const projectId = await trx('projects').insert({ ... });
  await trx('panels').insert({ project_id: projectId, ... });
});
```

### 4.4 마이그레이션

#### 생성
```bash
npx knex migrate:make create_users
```

#### 실행
```bash
# 최신 버전으로 마이그레이션
npx knex migrate:latest

# 롤백
npx knex migrate:rollback

# 특정 버전으로
npx knex migrate:up 001_create_users.js
```

---

## 5. 캐시 시스템

### 5.1 node-cache

#### 설치 및 설정
```bash
npm install node-cache
```

```javascript
// src/services/cacheService.js
const NodeCache = require('node-cache');

const cache = new NodeCache({
  stdTTL: 3600,           // 기본 TTL: 1시간
  checkperiod: 120,       // 만료 체크: 2분마다
  useClones: false,       // 성능 향상
  deleteOnExpire: true,   // 만료 시 자동 삭제
});

module.exports = {
  get: (key) => cache.get(key),
  set: (key, value, ttl) => cache.set(key, value, ttl),
  del: (key) => cache.del(key),
  flush: () => cache.flushAll(),
  keys: () => cache.keys(),
  has: (key) => cache.has(key),
};
```

#### 사용 예시
```javascript
const cache = require('./services/cacheService');

// 프로젝트 조회 시 캐싱
async function getProject(projectId) {
  const cacheKey = `project:${projectId}`;
  
  // 캐시 확인
  let project = cache.get(cacheKey);
  if (project) {
    return project;
  }
  
  // DB에서 조회
  project = await db('projects').where({ id: projectId }).first();
  
  // 캐시 저장 (1시간)
  cache.set(cacheKey, project, 3600);
  
  return project;
}

// 프로젝트 업데이트 시 캐시 무효화
async function updateProject(projectId, data) {
  await db('projects').where({ id: projectId }).update(data);
  
  // 캐시 삭제
  cache.del(`project:${projectId}`);
}
```

### 5.2 캐싱 전략

#### 1. 읽기 우선 (Read-Through)
```javascript
async function getWithCache(key, fetchFn, ttl = 3600) {
  let value = cache.get(key);
  if (value) return value;
  
  value = await fetchFn();
  cache.set(key, value, ttl);
  return value;
}

// 사용
const project = await getWithCache(
  `project:${id}`,
  () => db('projects').where({ id }).first(),
  3600
);
```

#### 2. 쓰기 우선 (Write-Through)
```javascript
async function updateWithCache(key, data, updateFn, ttl = 3600) {
  await updateFn(data);
  cache.set(key, data, ttl);
}
```

#### 3. 캐시 무효화 패턴
```javascript
// 특정 패턴의 모든 캐시 삭제
function invalidatePattern(pattern) {
  const keys = cache.keys();
  const matchedKeys = keys.filter(key => key.startsWith(pattern));
  matchedKeys.forEach(key => cache.del(key));
}

// 사용자의 모든 프로젝트 캐시 무효화
invalidatePattern(`projects:user:${userId}`);
```

---

## 6. 작업 큐

### 6.1 p-queue

#### 설치 및 설정
```bash
npm install p-queue
```

```javascript
// src/services/queueService.js
const PQueue = require('p-queue').default;

// TTS 생성 큐 (동시 5개)
const ttsQueue = new PQueue({
  concurrency: 5,
  autoStart: true,
});

// 이미지 처리 큐 (동시 3개)
const imageQueue = new PQueue({
  concurrency: 3,
  autoStart: true,
});

module.exports = {
  ttsQueue,
  imageQueue,
  
  // TTS 작업 추가
  addTTSJob: (task) => ttsQueue.add(task),
  
  // 이미지 작업 추가
  addImageJob: (task) => imageQueue.add(task),
  
  // 상태 확인
  getStatus: () => ({
    tts: {
      pending: ttsQueue.pending,
      size: ttsQueue.size,
    },
    image: {
      pending: imageQueue.pending,
      size: imageQueue.size,
    },
  }),
};
```

#### 사용 예시
```javascript
const { addTTSJob } = require('./services/queueService');
const ttsService = require('./services/ttsService');

// TTS 생성 요청
async function generatePanelTTS(panelId, script, voiceSettings) {
  return addTTSJob(async () => {
    console.log(`Processing TTS for panel ${panelId}`);
    
    // TTS 생성
    const audioBuffer = await ttsService.generate(script, voiceSettings);
    
    // S3 업로드 (또는 로컬 저장)
    const audioUrl = await uploadAudio(audioBuffer, panelId);
    
    // DB 업데이트
    await db('panels')
      .where({ id: panelId })
      .update({ audio_url: audioUrl });
    
    console.log(`TTS generated for panel ${panelId}: ${audioUrl}`);
    return audioUrl;
  });
}
```

### 6.2 우선순위 큐

```javascript
const PQueue = require('p-queue').default;

const priorityQueue = new PQueue({ concurrency: 5 });

// 높은 우선순위 (프리미엄 사용자)
priorityQueue.add(
  async () => generateTTS(data),
  { priority: 1 }
);

// 낮은 우선순위 (무료 사용자)
priorityQueue.add(
  async () => generateTTS(data),
  { priority: 10 }
);
```

### 6.3 진행률 추적

```javascript
const EventEmitter = require('events');

class TTSQueueManager extends EventEmitter {
  constructor() {
    super();
    this.queue = new PQueue({ concurrency: 5 });
    this.jobs = new Map();
  }
  
  async addJob(jobId, task) {
    this.jobs.set(jobId, {
      status: 'pending',
      progress: 0,
    });
    
    this.emit('job:started', jobId);
    
    try {
      await this.queue.add(async () => {
        this.updateProgress(jobId, 0);
        const result = await task((progress) => {
          this.updateProgress(jobId, progress);
        });
        this.updateProgress(jobId, 100);
        return result;
      });
      
      this.emit('job:completed', jobId);
    } catch (error) {
      this.emit('job:failed', jobId, error);
      throw error;
    } finally {
      this.jobs.delete(jobId);
    }
  }
  
  updateProgress(jobId, progress) {
    const job = this.jobs.get(jobId);
    if (job) {
      job.progress = progress;
      this.emit('job:progress', jobId, progress);
    }
  }
  
  getStatus(jobId) {
    return this.jobs.get(jobId);
  }
}

module.exports = new TTSQueueManager();
```

---

## 7. 개발 도구

### 7.1 필수 도구

#### VS Code Extensions
```
- ESLint
- Prettier
- Thunder Client (API 테스트)
- SQLite Viewer
- GitLens
- Error Lens
- Auto Rename Tag
- Path Intellisense
- npm Intellisense
```

#### 브라우저 확장
```
- React Developer Tools
- Redux DevTools
- Postman Interceptor
```

### 7.2 추천 도구

#### API 테스트
- **Postman** - 강력한 기능
- **Insomnia** - 깔끔한 UI
- **Thunder Client** - VS Code 내장

#### 데이터베이스 GUI
- **DB Browser for SQLite** - 무료, 강력
- **SQLite Studio** - 크로스 플랫폼
- **DBeaver** - 다중 DB 지원

#### Git 클라이언트
- **GitKraken** - 비주얼 Git
- **SourceTree** - 무료
- **GitHub Desktop** - 간단함

---

## 8. 배포 및 확장

### 8.1 로컬 → Production 전환

#### Database: SQLite → PostgreSQL
```javascript
// knexfile.js만 변경
module.exports = {
  production: {
    client: 'postgresql',
    connection: process.env.DATABASE_URL,
    // ... rest
  }
};
```

#### Cache: node-cache → Redis
```javascript
// cacheService.js 교체
const Redis = require('ioredis');
const redis = new Redis(process.env.REDIS_URL);

module.exports = {
  get: (key) => redis.get(key),
  set: (key, value, ttl) => redis.setex(key, ttl, JSON.stringify(value)),
  // ...
};
```

#### Queue: p-queue → Bull
```javascript
// queueService.js 교체
const Bull = require('bull');

const ttsQueue = new Bull('tts', process.env.REDIS_URL);

ttsQueue.process(async (job) => {
  // TTS 생성 로직
});
```

### 8.2 배포 옵션

#### 클라우드 서비스
```
- AWS (EC2, RDS, S3)
- Google Cloud
- Azure
- DigitalOcean
- Heroku (간단함)
- Vercel (Frontend)
```

#### 컨테이너화
```dockerfile
# Dockerfile
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .

EXPOSE 3001

CMD ["node", "server.js"]
```

```yaml
# docker-compose.yml
version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://...
    depends_on:
      - postgres
      - redis
  
  postgres:
    image: postgres:15-alpine
    # ...
  
  redis:
    image: redis:7-alpine
    # ...
```

---

## 9. 성능 고려사항

### 9.1 프론트엔드 최적화

```typescript
// 1. Code Splitting
const Dashboard = lazy(() => import('./pages/Dashboard'));

// 2. 메모이제이션
const MemoizedPanelItem = memo(PanelItem);

// 3. 가상 스크롤
<Virtuoso data={panels} itemContent={(index, panel) => ...} />

// 4. 디바운싱
const debouncedSave = useDebounce(savePanel, 1000);

// 5. 이미지 최적화
<img 
  src="/image.webp" 
  loading="lazy" 
  srcSet="..."
/>
```

### 9.2 백엔드 최적화

```javascript
// 1. 데이터베이스 인덱스
CREATE INDEX idx_panels_project ON panels(project_id);

// 2. N+1 쿼리 방지
const projects = await db('projects')
  .whereIn('id', projectIds)
  .select('*');

// 3. 캐싱
const cachedData = await getWithCache(key, fetchFn);

// 4. 압축
app.use(compression());

// 5. Rate Limiting
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 100
});
```

---

## 10. 보안 고려사항

### 10.1 인증 및 권한

```javascript
// JWT 검증
const verifyToken = (req, res, next) => {
  const token = req.headers.authorization?.split(' ')[1];
  if (!token) return res.status(401).json({ error: 'No token' });
  
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded;
    next();
  } catch (error) {
    res.status(401).json({ error: 'Invalid token' });
  }
};
```

### 10.2 입력 검증

```javascript
// Joi 스키마
const createProjectSchema = Joi.object({
  title: Joi.string().min(1).max(255).required(),
  description: Joi.string().max(1000).optional(),
  channel_name: Joi.string().max(100).optional(),
});

// 미들웨어
const validate = (schema) => (req, res, next) => {
  const { error } = schema.validate(req.body);
  if (error) {
    return res.status(400).json({ error: error.details[0].message });
  }
  next();
};
```

### 10.3 파일 업로드 보안

```javascript
const upload = multer({
  storage: multer.diskStorage({
    destination: './uploads/images',
    filename: (req, file, cb) => {
      const uniqueName = `${Date.now()}-${crypto.randomBytes(6).toString('hex')}${path.extname(file.originalname)}`;
      cb(null, uniqueName);
    },
  }),
  limits: {
    fileSize: 10 * 1024 * 1024, // 10MB
  },
  fileFilter: (req, file, cb) => {
    const allowedTypes = ['image/jpeg', 'image/png', 'image/webp'];
    if (allowedTypes.includes(file.mimetype)) {
      cb(null, true);
    } else {
      cb(new Error('Invalid file type'));
    }
  },
});
```

---

## 11. 2026-05-04 개정: 기능별 구현 기술 선택

현재 실제 `frontend/package.json`은 React 19, Vite 7, Tailwind 4 계열을 사용합니다. 기존 문서의 React 18/Vite 5 표기는 초기 계획으로 유지하되, 구현 기준은 실제 package 버전을 우선합니다.

### 11.1 영상 편집 MVP 기술 원칙

1. 로컬 MVP는 Express + SQLite 기반 모듈형 모놀리스로 유지합니다.
2. Gemini API 키, 파일 저장, FFmpeg 렌더링은 백엔드에서만 처리합니다.
3. 프론트는 9:16 프리뷰, 씬 타임라인, 인스펙터 UX에 집중합니다.
4. WebSocket은 1차 MVP 필수가 아닙니다. 처음에는 polling으로 작업 상태를 확인하고, 일괄 TTS/렌더가 안정화되면 Socket.io로 확장합니다.
5. 클라우드 저장소와 Redis/BullMQ는 production 전환 시점에 도입합니다.

### 11.2 기능별 기술 선택

| 기능 | MVP 기술 | 선택 이유 | 확장 후보 |
|------|----------|-----------|-----------|
| TTS 생성 | Gemini TTS `gemini-2.5-flash-preview-tts` | 자연어 지시문 기반 톤/페이스 제어, 한국어 지원, Gemini API 통합 | Gemini 3.1 TTS, custom voice, 사용량/비용 관리 |
| TTS 저장 | 로컬 `uploads/audio` + SQLite URL | 구현이 빠르고 렌더와 연결 쉬움 | S3/R2 + CDN |
| TTS 작업 상태 | SQLite 상태 필드 + polling | 단순하고 디버깅 쉬움 | Socket.io, BullMQ, Redis |
| 이미지 업로드 | Multer + Sharp | 현재 의존성에 이미 포함, 이미지 최적화 가능 | S3 direct upload, background removal |
| 영상 프리뷰 | React DOM + CSS aspect-ratio | 빠른 구현, 자막/이미지 검증에 충분 | Canvas, Remotion preview |
| 타임라인 | React state + dnd-kit | 현재 의존성 활용, 씬 정렬에 적합 | wavesurfer.js, virtualized timeline |
| 오디오 재생 | HTMLAudioElement | MVP에 충분하고 의존성 없음 | WaveSurfer.js waveform |
| 영상 렌더 | FFmpeg child process | 이미지+오디오+자막 mp4 생성 표준 | fluent-ffmpeg, cloud render worker |
| 작업 큐 | p-queue | 현재 의존성에 포함, 단일 서버에 적합 | BullMQ + Redis |
| 파일 메타데이터 | ffprobe 또는 music-metadata | duration 계산 필요 | media worker |
| 저장 상태 | Redux async state + debounce | 현재 구조와 잘 맞음 | offline queue, IndexedDB |

### 11.3 TTS 구현 표준

기본 모델은 `gemini-2.5-flash-preview-tts`로 시작합니다. Gemini TTS의 자연어 prompt와 prebuilt voice를 사용해 한국어 쇼츠 내레이션 톤을 제어합니다. Gemini TTS 응답은 24kHz PCM 오디오이므로 백엔드에서 FFmpeg로 mp3로 변환한 뒤 기존 렌더 파이프라인에 연결합니다.

```typescript
type TTSRequest = {
  sceneId: string;
  input: string;
  voice: 'Kore' | 'Puck' | 'Zephyr' | 'Charon' | 'Leda' | 'Sulafat';
  speed: number;
  instructions: string;
  responseFormat: 'mp3';
};
```

환경변수:

```bash
GEMINI_API_KEY=
GEMINI_TTS_MODEL=gemini-2.5-flash-preview-tts
TTS_DEFAULT_VOICE=Kore
TTS_RESPONSE_FORMAT=mp3
```

캐시 키:

```text
sha256(provider + model + voice + speed + instructions + normalizedScript)
```

### 11.4 렌더링 구현 표준

1차 export는 FFmpeg를 사용해 로컬에서 mp4를 생성합니다. 씬별 입력은 이미지, 오디오, 자막 스타일, duration으로 정규화하고, 최종 결과는 `uploads/renders`에 저장합니다.

```typescript
type RenderSceneInput = {
  sceneId: string;
  imagePath?: string;
  audioPath: string;
  script: string;
  durationMs: number;
  subtitle: {
    fontSize: number;
    color: string;
    backgroundColor: string;
    position: 'top' | 'middle' | 'bottom';
  };
  transition: {
    type: 'none' | 'fade';
    durationMs: number;
  };
};
```

### 11.5 도입 보류 기술

| 기술 | 보류 이유 | 도입 시점 |
|------|-----------|-----------|
| Redis/BullMQ | 로컬 MVP에는 운영 복잡도가 큼 | 일괄 TTS/렌더 작업량 증가 시 |
| PostgreSQL | 현재 SQLite로 충분 | 사용자/협업/배포 도입 시 |
| S3/CDN | 로컬 파일 저장으로 MVP 검증 가능 | 외부 배포 전 |
| Kubernetes/API Gateway | 현재 규모에 과함 | 서비스 분리 후 |
| Remotion | 좋은 선택지지만 초기 렌더 파이프라인 학습 비용 있음 | FFmpeg MVP 이후 |

---

## 12. 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| 1.0  | 2025-10-06 | AI Assistant | 초기 작성 |
|      |            |              | - React + Node.js 스택 정의 |
|      |            |              | - SQLite, node-cache, p-queue 선택 |
|      |            |              | - 로컬 개발 환경 최적화 |
| 1.1  | 2026-05-04 | Codex | 영상 편집 MVP 기능별 기술 선택 추가 |
|      |            |       | - TTS, 이미지, 프리뷰, 타임라인, FFmpeg 렌더 기술 기준 정의 |

---

**문서 끝**

이 문서는 SSUL MAKER의 로컬 개발 환경을 위한 기술 스택을 상세히 설명합니다.


