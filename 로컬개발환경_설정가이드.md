# SSUL MAKER 로컬 개발환경 설정 가이드

이 문서는 현재 저장소 코드 기준으로 새 PC에서 개발환경을 구성하는 절차를 설명합니다. 프로젝트 진행 상태와 인수인계 정보는 `HANDOFF.md`를 먼저 확인합니다.

## 1. 현재 기술 구성

### Frontend

- React 19
- TypeScript 5.9
- Vite 7
- Tailwind CSS 4
- Redux Toolkit
- Axios
- dnd-kit

### Backend

- Node.js + Express 5
- SQLite + Knex.js
- Gemini TTS API
- Multer + Sharp
- FFmpeg Static
- music-metadata

## 2. 사전 준비

필수:

- Git
- Node.js와 npm
- Gemini API 키

확인 명령:

```powershell
git --version
node --version
npm --version
```

다른 PC의 `node_modules`를 복사하지 않습니다. `better-sqlite3`와 `sharp`가 현재 PC 환경에 맞는 바이너리를 설치해야 합니다.

## 3. 저장소 복제

```powershell
git clone https://github.com/himangga01/short-video-service.git
cd short-video-service
git switch main
git pull --ff-only origin main
```

## 4. Backend 설정

### 환경변수

```powershell
Copy-Item backend\.env.example backend\.env
```

`backend/.env`:

```dotenv
PORT=3001
NODE_ENV=development
DATABASE_PATH=./data/ssulmaker.db
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
FRONTEND_URL=http://localhost:5173

GEMINI_API_KEY=your_api_key
GEMINI_TTS_MODEL=gemini-2.5-flash-preview-tts
GEMINI_TTS_TIMEOUT_MS=60000
TTS_DEFAULT_VOICE=Kore
TTS_DEFAULT_INSTRUCTIONS=한국어 썰 영상 내레이션처럼 자연스럽고 몰입감 있게 읽어주세요.
```

### 설치와 migration

```powershell
cd backend
npm ci
npm run migrate
```

### 실행

```powershell
npm run dev
```

확인 주소:

- API: `http://localhost:3001`
- Health: `http://localhost:3001/health`
- Uploads: `http://localhost:3001/uploads/...`

### 네이티브 모듈 오류

Node.js 버전 변경 후 `better-sqlite3` 바이너리 오류가 발생하면 다음을 실행합니다.

```powershell
cd backend
npm rebuild better-sqlite3
```

## 5. Frontend 설정

### 환경변수

`frontend/.env`를 생성합니다.

```dotenv
VITE_API_URL=http://127.0.0.1:3001
```

API 주소에는 `/api`를 붙이지 않습니다. 각 API 클라이언트가 `/api/projects`, `/api/panels`, `/api/render` 경로를 추가합니다.

### 설치와 실행

```powershell
cd frontend
npm ci
npm run dev
```

확인 주소:

- App: `http://localhost:5173`

## 6. 동시 실행 순서

터미널 1:

```powershell
cd backend
npm run dev
```

터미널 2:

```powershell
cd frontend
npm run dev
```

Frontend가 열리지만 프로젝트가 로드되지 않으면 Backend health와 `frontend/.env`의 `VITE_API_URL`을 먼저 확인합니다.

## 7. 로컬 데이터 경로

```text
backend/data/ssulmaker.db       # SQLite DB
backend/uploads/images/         # 업로드 이미지
backend/uploads/audio/          # Gemini TTS MP3
backend/uploads/renders/        # Export MP4
```

위 경로와 `.env`, `node_modules`, `dist`, 로그는 Git에 포함되지 않습니다.

기존 PC의 프로젝트와 미디어를 유지해야 한다면 서버를 종료한 상태에서 `backend/data`와 `backend/uploads`를 새 PC의 같은 경로로 함께 복사합니다. 새 환경으로 시작하면 복사하지 않고 migration만 실행합니다.

## 8. 사용 가능한 명령

### Backend

```powershell
cd backend
npm run dev
npm start
npm run migrate
npm run migrate:rollback
npm test
```

### Frontend

```powershell
cd frontend
npm run dev
npm run lint
npm run build
npm run preview
```

테스트와 수동 확인 절차는 `TESTING.md`를 따릅니다.

## 9. 자주 발생하는 문제

### CORS 오류

- Backend가 `3001`에서 실행 중인지 확인합니다.
- Frontend 주소가 `http://localhost:5173` 또는 `http://127.0.0.1:5173`인지 확인합니다.
- 다른 포트를 사용하면 `backend/.env`의 `FRONTEND_URL`도 맞춥니다.

### 프로젝트 목록이 비어 있음

- `http://localhost:3001/health` 확인
- `http://localhost:3001/api/projects` 확인
- 새 PC에 기존 SQLite DB를 복사했는지 확인

### TTS 실패

- `backend/.env`의 `GEMINI_API_KEY` 확인
- `GEMINI_TTS_MODEL=gemini-2.5-flash-preview-tts` 확인
- 네트워크와 timeout 로그 확인

### 이미지 또는 렌더 결과가 없음

- DB만 복사하고 `backend/uploads`를 복사하지 않았는지 확인합니다.
- `UPLOAD_DIR`이 기본 `./uploads` 또는 의도한 절대경로인지 확인합니다.

## 10. Git 작업 시작과 종료

시작:

```powershell
git switch main
git pull --ff-only origin main
git switch -c <type>/<short-description>
```

종료 시:

- 변경 내역과 남은 이슈를 `work-log.md`에 기록합니다.
- 새 환경변수는 실제 키가 아니라 `.env.example`에 빈 값/예시값으로 추가합니다.
- DB와 uploads는 커밋하지 않습니다.
- 사용자 요청 범위에 따라 명시한 파일만 stage하고 commit/push합니다.

## AI / Developer Quick Reference

### Clone and Bootstrap

```text
git clone -> switch main -> npm ci in backend/frontend -> configure env -> migrate -> run both dev servers
```

### Runtime Endpoints

- Frontend: `http://localhost:5173`
- Backend: `http://localhost:3001`
- Health: `GET /health`
- API namespaces: `/api/projects`, `/api/panels`, `/api/render`
- Media namespace: `/uploads`

### Environment Contract

- Backend secrets/config: `backend/.env`
- Frontend API base URL: `frontend/.env`
- `VITE_API_URL` is the server origin without `/api`.
- Current TTS model: `gemini-2.5-flash-preview-tts`.

### Local Persistence

- SQLite: `backend/data/ssulmaker.db`
- Media: `backend/uploads/{images,audio,renders}`
- Neither is version-controlled; copy both when preserving prior local content.

### Canonical References

- Handoff/current priorities: `HANDOFF.md`
- Product overview: `README.md`
- Verification procedures: `TESTING.md`
- Historical work: `work-log.md`
