# 🔍 SSUL MAKER 개선 필요사항 분석서

## 📌 문서 정보
- **작성일**: 2025-10-06
- **목적**: 현재 설계의 부족한 부분 파악 및 개선 방안 제시
- **분석 대상**: 요구사항서, 설계서, UX/UI 기능설명서

---

## 목차
1. [UX/UI 사용성 개선 필요사항](#1-uxui-사용성-개선-필요사항)
2. [기능적 개선 필요사항](#2-기능적-개선-필요사항)
3. [기술적 개선 필요사항](#3-기술적-개선-필요사항)
4. [비즈니스 로직 개선 필요사항](#4-비즈니스-로직-개선-필요사항)
5. [보안 및 데이터 관리 개선](#5-보안-및-데이터-관리-개선)
6. [성능 및 확장성 개선](#6-성능-및-확장성-개선)
7. [우선순위 및 구현 순서](#7-우선순위-및-구현-순서)

---

## 1. UX/UI 사용성 개선 필요사항

### 1.1 네비게이션 및 정보 구조

#### ❌ 부족한 부분
1. **브레드크럼(Breadcrumb) 없음**
   - 현재 위치를 파악하기 어려움
   - 프로젝트 > 패널 계층 구조가 명확하지 않음

2. **검색 기능 미흡**
   - 검색 아이콘만 있고 구체적인 기능 없음
   - 프로젝트/패널 내용 검색 불가
   - 필터링 옵션 부재

3. **최근 작업 내역 없음**
   - 최근에 편집한 프로젝트/패널 표시 없음
   - 작업 히스토리 추적 불가

#### ✅ 개선 방안

**A. 브레드크럼 추가**
```
홈 > 만생지원금 2차는 못받는다? > 패널 3
```

**B. 통합 검색 시스템**
```typescript
interface SearchFeature {
  types: ['project', 'panel', 'preset'];
  filters: {
    date: 'recent' | 'week' | 'month' | 'all';
    status: 'draft' | 'completed';
    author: string;
  };
  realTimeSearch: boolean;
  searchHistory: string[];
}
```

**C. 최근 작업 위젯**
```
┌────────────────────────────────┐
│ 최근 작업                       │
├────────────────────────────────┤
│ 📁 만생지원금... (5분 전)      │
│ 📁 에어컨 커튼... (1시간 전)   │
│ 📁 새 프로젝트 (어제)          │
└────────────────────────────────┘
```

---

### 1.2 패널 편집 UX

#### ❌ 부족한 부분
1. **일괄 편집 기능 없음**
   - 여러 패널의 음성 설정을 한 번에 변경 불가
   - 스타일 일괄 적용 불가

2. **실시간 협업 기능 없음**
   - 다른 사용자의 편집 상태 표시 없음
   - 충돌 해결 메커니즘 부재

3. **패널 복사/붙여넣기 불명확**
   - 패널 복제 기능 언급 없음
   - 템플릿화 기능 부재

4. **Undo/Redo 기능 명시 없음**
   - 실수 복구 방법 불명확
   - 버전 관리 미흡

#### ✅ 개선 방안

**A. 일괄 편집 모드**
```
┌────────────────────────────────────────┐
│ ☑️ 패널 1, 2, 3 선택됨 (3개)           │
│                                        │
│ [일괄 설정]                            │
│ - 음성: Jiny → 변경                   │
│ - 텍스트 크기: 22 → 변경              │
│ - 배경색: #000000 → 변경              │
│                                        │
│ [적용] [취소]                          │
└────────────────────────────────────────┘
```

**B. 실시간 협업 인디케이터**
```typescript
interface CollaborationFeature {
  activeUsers: {
    userId: string;
    userName: string;
    currentPanel: string;
    cursorPosition: { x: number; y: number };
    color: string;
  }[];
  lockMechanism: 'optimistic' | 'pessimistic';
  conflictResolution: 'last-write-wins' | 'manual-merge';
}
```

**C. 패널 관리 개선**
```
우클릭 메뉴:
┌────────────────────┐
│ ✏️ 편집            │
│ 📋 복사            │
│ 📑 복제            │
│ ⭐ 템플릿으로 저장 │
│ 🗑️ 삭제            │
└────────────────────┘
```

**D. Undo/Redo 스택**
```typescript
interface HistoryFeature {
  maxStackSize: 50;
  actions: HistoryAction[];
  currentIndex: number;
  shortcuts: {
    undo: 'Ctrl+Z';
    redo: 'Ctrl+Y';
  };
}
```

---

### 1.3 피드백 및 상태 표시

#### ❌ 부족한 부분
1. **로딩 상태 불명확**
   - 어떤 작업이 진행 중인지 불명확
   - 예상 소요 시간 표시 없음

2. **에러 처리 일관성 부족**
   - 에러 메시지 위치가 일관되지 않음
   - 복구 방법 제시 미흡

3. **성공 피드백 부족**
   - 저장 완료, TTS 생성 완료 등의 피드백이 약함
   - 시각적/청각적 피드백 부족

4. **네트워크 상태 표시 없음**
   - 오프라인 상태 감지 없음
   - 연결 끊김 시 대응 방안 부재

#### ✅ 개선 방안

**A. 통합 로딩 인디케이터**
```
┌────────────────────────────────────────┐
│ 🔄 패널 3의 TTS를 생성하는 중...       │
│ ████████████░░░░░░░░  60%              │
│ 약 15초 남음                           │
└────────────────────────────────────────┘
```

**B. 에러 알림 시스템**
```typescript
interface ErrorNotification {
  type: 'error' | 'warning' | 'info';
  title: string;
  message: string;
  actions: {
    label: string;
    handler: () => void;
  }[];
  autoClose: number | false;
  position: 'top-right' | 'bottom-center';
}
```

**C. 성공 애니메이션**
```css
/* 저장 완료 */
.save-success {
  animation: checkmarkPop 500ms ease-out;
}

@keyframes checkmarkPop {
  0% { transform: scale(0); }
  50% { transform: scale(1.2); }
  100% { transform: scale(1); }
}
```

**D. 네트워크 상태 바**
```
┌────────────────────────────────────────┐
│ ⚠️ 인터넷 연결이 끊어졌습니다           │
│ 작업 내용은 로컬에 저장되고 있습니다    │
│ 연결이 복구되면 자동으로 동기화됩니다   │
└────────────────────────────────────────┘
```

---

### 1.4 모바일 UX

#### ❌ 부족한 부분
1. **터치 제스처 미흡**
   - 스와이프로 패널 삭제 등의 제스처 없음
   - 핀치 줌 등 모바일 전용 제스처 부재

2. **모바일 메뉴 구조 불명확**
   - 햄버거 메뉴 내용 명시 없음
   - 하단 네비게이션 부재

3. **키보드 처리 미흡**
   - 가상 키보드 표시 시 레이아웃 대응 없음
   - 입력 필드 포커스 시 스크롤 문제

#### ✅ 개선 방안

**A. 터치 제스처**
```typescript
interface TouchGestures {
  swipeLeft: 'nextPanel' | 'delete';
  swipeRight: 'previousPanel' | 'back';
  longPress: 'showContextMenu';
  doubleTap: 'edit';
  pinchZoom: 'zoomText' | 'zoomImage';
}
```

**B. 모바일 하단 네비게이션**
```
┌────────────────────────────────────────┐
│                                        │
│         (Main Content)                 │
│                                        │
└────────────────────────────────────────┘
┌────────────────────────────────────────┐
│  🏠     📁      ➕      ⚙️      👤    │
│  홈   프로젝트  추가   설정   프로필   │
└────────────────────────────────────────┘
```

**C. 키보드 대응**
```typescript
interface KeyboardHandler {
  adjustLayout: true;
  scrollToFocus: true;
  hideToolbarWhenKeyboardOpen: true;
  minVisibleHeight: 200; // px
}
```

---

## 2. 기능적 개선 필요사항

### 2.1 프로젝트 관리

#### ❌ 부족한 부분
1. **프로젝트 구조화 부족**
   - 폴더/카테고리 시스템 없음
   - 태그 기능 없음
   - 프로젝트 그룹핑 불가

2. **프로젝트 템플릿 부족**
   - 사용자 정의 템플릿 저장 불가
   - 공유 템플릿 마켓플레이스 없음

3. **버전 관리 미흡**
   - 프로젝트 버전 관리 없음
   - 특정 시점으로 복구 불가

4. **공유 및 협업 기능 부재**
   - 프로젝트 공유 링크 생성 불가
   - 권한 관리 시스템 없음

#### ✅ 개선 방안

**A. 폴더/태그 시스템**
```
프로젝트 구조:
📁 유튜브 콘텐츠
  ├─ 📁 시사/뉴스
  │   ├─ 만생지원금 2차... #정치 #경제
  │   └─ 에어컨 요금... #생활팁 #절약
  └─ 📁 리뷰
      └─ 제품 리뷰... #리뷰 #추천
```

**B. 템플릿 시스템**
```typescript
interface TemplateFeature {
  userTemplates: Template[];
  publicTemplates: Template[];
  templateMarketplace: {
    trending: Template[];
    categories: string[];
    search: (query: string) => Template[];
  };
  saveAsTemplate: (project: Project) => Template;
  applyTemplate: (template: Template, project: Project) => void;
}
```

**C. 버전 관리**
```
┌────────────────────────────────────────┐
│ 📜 버전 히스토리                        │
├────────────────────────────────────────┤
│ v3 (현재) - 2025-10-06 15:30          │
│   "이미지 7개 추가"                    │
│                                        │
│ v2 - 2025-10-06 14:20                 │
│   "스크립트 수정"                      │
│                                        │
│ v1 - 2025-10-06 10:00                 │
│   "초기 생성"                          │
│                                        │
│ [버전 비교] [특정 버전으로 복구]       │
└────────────────────────────────────────┘
```

**D. 공유 및 권한 관리**
```typescript
interface SharingFeature {
  shareLink: {
    url: string;
    expiresAt?: Date;
    password?: string;
    permissions: 'view' | 'comment' | 'edit';
  };
  collaborators: {
    userId: string;
    role: 'owner' | 'editor' | 'viewer';
    invitedAt: Date;
  }[];
  publicAccess: boolean;
}
```

---

### 2.2 패널 편집 기능

#### ❌ 부족한 부분
1. **텍스트 편집 기능 제한적**
   - 리치 텍스트 편집 불가 (볼드, 이탤릭 등)
   - 특수 효과 (강조, 일시정지 등) 없음
   - 발음 기호 추가 불가

2. **오디오 편집 기능 부재**
   - TTS 생성 후 편집 불가
   - 볼륨 조절 불가
   - 페이드 인/아웃 효과 없음
   - 배경음악 추가 불가

3. **이미지 편집 기능 제한적**
   - 기본 크롭만 가능
   - 필터/효과 부족
   - 애니메이션 효과 없음
   - 다중 이미지 지원 불명확

4. **타임라인 편집 부재**
   - 패널 간 전환 효과 없음
   - 타이밍 조절 불가
   - 오버랩/크로스페이드 등 없음

#### ✅ 개선 방안

**A. 리치 텍스트 에디터**
```typescript
interface RichTextEditor {
  formatting: {
    bold: boolean;
    italic: boolean;
    underline: boolean;
    color: string;
    backgroundColor: string;
  };
  specialEffects: {
    emphasis: 'normal' | 'strong' | 'soft';
    pause: number; // ms
    speedModifier: number; // 0.5 ~ 2.0
  };
  pronunciation: {
    word: string;
    phonetic: string;
  }[];
}

// 사용 예시
"안녕하세요! [강조:강]여러분[/강조] [일시정지:500ms]"
```

**B. 오디오 편집 기능**
```typescript
interface AudioEditor {
  effects: {
    volume: number; // 0 ~ 200%
    fadeIn: number; // ms
    fadeOut: number; // ms
    speed: number; // 0.5 ~ 2.0
    pitch: number; // -12 ~ +12 semitones
  };
  backgroundMusic: {
    url: string;
    volume: number;
    startTime: number;
    fadeIn: number;
    fadeOut: number;
  } | null;
  trim: {
    start: number;
    end: number;
  };
  merge: (clips: AudioClip[]) => AudioClip;
}
```

**C. 이미지 고급 편집**
```typescript
interface ImageEditor {
  crop: {
    x: number;
    y: number;
    width: number;
    height: number;
    aspectRatio?: string; // '16:9', '4:3', '1:1'
  };
  filters: {
    brightness: number; // -100 ~ 100
    contrast: number; // -100 ~ 100
    saturation: number; // -100 ~ 100
    blur: number; // 0 ~ 100
    sepia: number; // 0 ~ 100
  };
  effects: {
    kenBurns: { // 켄 번즈 효과
      startZoom: number;
      endZoom: number;
      duration: number;
    } | null;
    pan: {
      direction: 'left' | 'right' | 'up' | 'down';
      distance: number;
    } | null;
  };
  multipleImages: {
    layout: 'single' | 'split' | 'grid';
    images: string[];
    transition: 'fade' | 'slide' | 'zoom';
  };
}
```

**D. 타임라인 에디터**
```typescript
interface TimelineEditor {
  panels: {
    id: string;
    startTime: number;
    duration: number;
    transition: {
      type: 'cut' | 'fade' | 'slide' | 'zoom';
      duration: number;
    };
  }[];
  totalDuration: number;
  playback: {
    currentTime: number;
    playing: boolean;
    loop: boolean;
  };
  markers: {
    time: number;
    label: string;
  }[];
}
```

---

### 2.3 TTS 기능

#### ❌ 부족한 부분
1. **음성 옵션 제한적**
   - 목소리 선택지 부족 (Jiny만?)
   - 감정 표현 불가
   - 다국어 TTS 명시 없음

2. **발음 커스터마이징 부족**
   - 특정 단어 발음 수정 불가
   - 외래어/전문용어 발음 가이드 없음
   - 발음 사전 기능 부재

3. **TTS 품질 관리 미흡**
   - 생성된 TTS 품질 평가 없음
   - 재생성 기준 불명확
   - A/B 테스트 기능 없음

4. **TTS 비용 관리 부재**
   - 글자 수 카운팅 없음
   - 예상 비용 표시 없음
   - 사용량 제한 알림 없음

#### ✅ 개선 방안

**A. 다양한 음성 옵션**
```typescript
interface VoiceOptions {
  voices: {
    id: string;
    name: string;
    language: string;
    gender: 'male' | 'female' | 'neutral';
    age: 'child' | 'young' | 'adult' | 'senior';
    style: 'casual' | 'formal' | 'cheerful' | 'serious';
    preview: string; // 샘플 오디오 URL
  }[];
  emotions: {
    type: 'neutral' | 'happy' | 'sad' | 'angry' | 'excited';
    intensity: number; // 0 ~ 100
  };
  multilingual: {
    autoDetect: boolean;
    languages: string[];
  };
}
```

**B. 발음 사전**
```typescript
interface PronunciationDictionary {
  entries: {
    word: string;
    pronunciation: string;
    phoneticSymbol?: string;
    audioSample?: string;
  }[];
  autoReplace: boolean;
  globalDictionary: boolean;
  projectDictionary: boolean;
}

// UI 예시
"에어컨 → [에어콘] (사전 적용)"
```

**C. TTS 품질 관리**
```typescript
interface QualityControl {
  rating: {
    score: number; // 1-5
    feedback: string;
  };
  regenerateOptions: {
    reason: 'pronunciation' | 'speed' | 'emotion' | 'other';
    specificIssue: string;
  };
  abTesting: {
    variantA: string; // audio URL
    variantB: string; // audio URL
    selectWinner: (variant: 'A' | 'B') => void;
  };
}
```

**D. 비용 및 사용량 관리**
```
┌────────────────────────────────────────┐
│ 💰 TTS 사용량                          │
├────────────────────────────────────────┤
│ 이번 달 사용: 15,234자 / 50,000자      │
│ █████████████░░░░░░░  30%              │
│                                        │
│ 예상 비용: $3.05 / $10.00              │
│                                        │
│ 현재 패널: 156자 (약 $0.03)            │
│                                        │
│ [플랜 업그레이드] [상세 내역]          │
└────────────────────────────────────────┘
```

---

### 2.4 프리셋 시스템

#### ❌ 부족한 부분
1. **프리셋 관리 미흡**
   - 사용자 정의 프리셋 저장 불가
   - 프리셋 수정 불가
   - 프리셋 공유 불가

2. **프리셋 적용 유연성 부족**
   - 전체 적용만 가능 (부분 적용 불가)
   - 프리셋 미리보기 없음
   - 프리셋 비교 기능 없음

3. **프리셋 카테고리화 부족**
   - 프리셋 분류 불명확
   - 검색/필터 기능 없음
   - 추천 프리셋 없음

#### ✅ 개선 방안

**A. 프리셋 관리 시스템**
```typescript
interface PresetManagement {
  create: (project: Project) => Preset;
  edit: (presetId: string, changes: Partial<Preset>) => void;
  delete: (presetId: string) => void;
  share: {
    generateLink: (presetId: string) => string;
    visibility: 'private' | 'team' | 'public';
  };
  import: (presetUrl: string) => Preset;
}
```

**B. 프리셋 적용 옵션**
```
┌────────────────────────────────────────┐
│ 프리셋 적용: "에어컨 요금 폭탄"        │
├────────────────────────────────────────┤
│ 적용할 항목:                           │
│ ☑️ 스크립트 구조 (7개 패널)            │
│ ☑️ 텍스트 스타일                       │
│ ☑️ 음성 설정                           │
│ ☐ 이미지 (가이드만)                   │
│ ☑️ 프로젝트 정보                       │
│                                        │
│ [미리보기] [적용] [취소]               │
└────────────────────────────────────────┘
```

**C. 프리셋 마켓플레이스**
```
┌────────────────────────────────────────┐
│ 🏪 프리셋 마켓플레이스                  │
├────────────────────────────────────────┤
│ 🔥 인기                                │
│ ├─ 에어컨 요금 폭탄 (사용 1.2K)        │
│ ├─ 생활 꿀팁 (사용 856)                │
│ └─ 뉴스 브리핑 (사용 623)              │
│                                        │
│ 📁 카테고리                             │
│ ├─ 생활/팁                             │
│ ├─ 시사/뉴스                           │
│ ├─ 리뷰                                │
│ └─ 교육                                │
│                                        │
│ ⭐ 내 프리셋 (3개)                      │
└────────────────────────────────────────┘
```

---

## 3. 기술적 개선 필요사항

### 3.1 데이터 동기화

#### ❌ 부족한 부분
1. **오프라인 지원 없음**
   - 네트워크 끊김 시 작업 불가
   - 로컬 저장 메커니즘 불명확

2. **실시간 동기화 미흡**
   - 여러 디바이스 간 동기화 없음
   - 충돌 해결 메커니즘 부재

3. **데이터 백업 불명확**
   - 자동 백업 주기 불명확
   - 사용자 백업 다운로드 불가

#### ✅ 개선 방안

**A. 오프라인 지원**
```typescript
interface OfflineSupport {
  serviceWorker: {
    cacheStrategy: 'network-first' | 'cache-first' | 'stale-while-revalidate';
    cacheDuration: number;
  };
  indexedDB: {
    syncQueue: SyncJob[];
    localProjects: Project[];
    conflictResolution: 'server' | 'local' | 'manual';
  };
  syncOnReconnect: boolean;
}
```

**B. 멀티 디바이스 동기화**
```typescript
interface MultiDeviceSync {
  devices: {
    id: string;
    name: string;
    lastSync: Date;
    online: boolean;
  }[];
  conflictDetection: {
    strategy: 'timestamp' | 'version-vector' | 'crdt';
    autoResolve: boolean;
  };
  selectiveSync: {
    projects: string[];
    presets: boolean;
    settings: boolean;
  };
}
```

**C. 백업 시스템**
```typescript
interface BackupSystem {
  auto: {
    enabled: boolean;
    frequency: 'hourly' | 'daily' | 'weekly';
    retention: number; // days
  };
  manual: {
    export: () => Blob;
    import: (file: File) => Promise<void>;
  };
  cloud: {
    provider: 'aws' | 'google' | 'dropbox';
    enabled: boolean;
  };
}
```

---

### 3.2 API 및 통합

#### ❌ 부족한 부분
1. **외부 통합 부족**
   - 유튜브 직접 업로드 없음
   - 소셜 미디어 연동 없음
   - Zapier/IFTTT 통합 없음

2. **Webhook 지원 없음**
   - 이벤트 알림 없음
   - 자동화 트리거 없음

3. **공개 API 부재**
   - 개발자 API 문서 없음
   - API 키 관리 없음

#### ✅ 개선 방안

**A. 외부 플랫폼 통합**
```typescript
interface ExternalIntegrations {
  youtube: {
    enabled: boolean;
    uploadDirect: boolean;
    defaultSettings: {
      title: string;
      description: string;
      tags: string[];
      category: string;
      privacy: 'public' | 'unlisted' | 'private';
    };
  };
  socialMedia: {
    platforms: ('instagram' | 'tiktok' | 'facebook')[];
    crossPost: boolean;
  };
  automation: {
    zapier: boolean;
    ifttt: boolean;
    webhooks: Webhook[];
  };
}
```

**B. Webhook 시스템**
```typescript
interface WebhookSystem {
  webhooks: {
    id: string;
    url: string;
    events: ('project.created' | 'tts.completed' | 'panel.updated')[];
    secret: string;
    active: boolean;
  }[];
  retryPolicy: {
    maxRetries: number;
    backoff: 'exponential' | 'linear';
  };
  logs: WebhookLog[];
}
```

**C. 개발자 API**
```typescript
interface DeveloperAPI {
  authentication: {
    type: 'api-key' | 'oauth2';
    scopes: string[];
  };
  rateLimit: {
    requestsPerMinute: number;
    requestsPerDay: number;
  };
  sdk: {
    languages: ('javascript' | 'python' | 'go')[];
    documentation: string;
  };
}
```

---

### 3.3 성능 최적화

#### ❌ 부족한 부분
1. **이미지 최적화 부족**
   - 이미지 압축 전략 불명확
   - 레이지 로딩 언급 없음
   - WebP 포맷 지원 불명확

2. **코드 스플리팅 미흡**
   - 번들 크기 최적화 언급 부족
   - 동적 임포트 전략 없음

3. **캐싱 전략 부족**
   - CDN 캐싱 정책 불명확
   - 브라우저 캐싱 전략 없음

#### ✅ 개선 방안

**A. 이미지 최적화**
```typescript
interface ImageOptimization {
  upload: {
    maxSize: number; // 10MB
    autoCompress: boolean;
    targetSize: number; // 1MB
    quality: number; // 80
  };
  formats: {
    webp: boolean;
    avif: boolean;
    fallback: 'jpg' | 'png';
  };
  responsive: {
    breakpoints: number[];
    generateSrcSet: boolean;
  };
  lazyLoading: {
    enabled: boolean;
    threshold: number; // pixels
    placeholder: 'blur' | 'color';
  };
}
```

**B. 번들 최적화**
```typescript
// webpack.config.js
module.exports = {
  optimization: {
    splitChunks: {
      chunks: 'all',
      cacheGroups: {
        vendor: {
          test: /[\\/]node_modules[\\/]/,
          priority: -10,
        },
        common: {
          minChunks: 2,
          priority: -20,
        },
      },
    },
  },
  performance: {
    maxAssetSize: 500000, // 500KB
    maxEntrypointSize: 500000,
  },
};

// Dynamic imports
const AudioEditor = lazy(() => import('./components/AudioEditor'));
const ImageEditor = lazy(() => import('./components/ImageEditor'));
```

**C. 캐싱 전략**
```typescript
interface CachingStrategy {
  cdn: {
    staticAssets: {
      maxAge: 31536000, // 1 year
      immutable: true,
    };
    apiResponses: {
      maxAge: 300, // 5 minutes
      staleWhileRevalidate: 600,
    };
  };
  servicWorker: {
    strategies: {
      images: 'cache-first',
      api: 'network-first',
      static: 'cache-first',
    };
  };
  browser: {
    localStorage: {
      projects: true;
      settings: true;
      maxSize: 5 * 1024 * 1024, // 5MB
    };
  };
}
```

---

## 4. 비즈니스 로직 개선 필요사항

### 4.1 사용자 관리

#### ❌ 부족한 부분
1. **팀/조직 관리 없음**
   - 팀 생성 불가
   - 역할 기반 접근 제어 부족
   - 팀 사용량 관리 없음

2. **사용자 분석 부족**
   - 사용 패턴 추적 없음
   - 리텐션 메트릭 없음
   - 사용자 세그멘테이션 없음

#### ✅ 개선 방안

**A. 팀 관리 시스템**
```typescript
interface TeamManagement {
  team: {
    id: string;
    name: string;
    plan: 'team' | 'business' | 'enterprise';
    members: {
      userId: string;
      role: 'owner' | 'admin' | 'editor' | 'viewer';
      invitedAt: Date;
      lastActive: Date;
    }[];
  };
  permissions: {
    [role: string]: {
      projects: ('create' | 'read' | 'update' | 'delete')[];
      presets: ('create' | 'read' | 'update' | 'delete')[];
      team: ('invite' | 'remove' | 'changeRole')[];
    };
  };
  usage: {
    ttsCharacters: number;
    storageUsed: number;
    apiCalls: number;
    limits: {
      ttsCharacters: number;
      storage: number;
      apiCalls: number;
    };
  };
}
```

**B. 사용자 분석 대시보드**
```
┌────────────────────────────────────────┐
│ 📊 사용 통계                            │
├────────────────────────────────────────┤
│ 이번 주 활동                           │
│ - 프로젝트 생성: 3개                   │
│ - TTS 생성: 45개                       │
│ - 총 작업 시간: 4시간 23분              │
│                                        │
│ 인기 기능                              │
│ 1. TTS 생성 (85%)                     │
│ 2. 이미지 편집 (62%)                   │
│ 3. 프리셋 사용 (43%)                   │
│                                        │
│ 생산성 팁                              │
│ 💡 프리셋을 활용하면 작업 시간을        │
│    40% 단축할 수 있습니다!             │
└────────────────────────────────────────┘
```

---

### 4.2 구독 및 결제

#### ❌ 부족한 부분
1. **구독 플랜 상세 없음**
   - 플랜별 기능 차이 불명확
   - 업그레이드 플로우 없음
   - 프로모션 코드 시스템 없음

2. **사용량 기반 과금 없음**
   - TTS 생성 수에 따른 추가 요금 없음
   - 종량제 옵션 없음

3. **결제 이력 관리 부족**
   - 영수증 다운로드 불가
   - 결제 내역 조회 불명확

#### ✅ 개선 방안

**A. 구독 플랜 시스템**
```typescript
interface SubscriptionPlans {
  plans: {
    id: string;
    name: 'free' | 'basic' | 'pro' | 'business';
    price: {
      monthly: number;
      yearly: number;
      discount: number; // 연간 할인율
    };
    features: {
      ttsCharactersPerMonth: number;
      projectsLimit: number;
      storageGB: number;
      voiceOptions: number;
      teamMembers: number;
      apiAccess: boolean;
      prioritySupport: boolean;
      customBranding: boolean;
    };
  }[];
  addOns: {
    extraTTS: {
      characters: number;
      price: number;
    };
    extraStorage: {
      gb: number;
      price: number;
    };
  };
  promotions: {
    code: string;
    discountPercent: number;
    expiresAt: Date;
    maxUses: number;
  }[];
}
```

**B. 사용량 및 청구 대시보드**
```
┌────────────────────────────────────────┐
│ 💳 청구 및 사용량                       │
├────────────────────────────────────────┤
│ 현재 플랜: Pro ($19/월)                │
│ 다음 결제일: 2025-11-06                │
│                                        │
│ 이번 달 사용량                          │
│ ├─ TTS: 35,248 / 50,000자 (70%)       │
│ ├─ 저장공간: 3.2GB / 10GB (32%)        │
│ └─ API 호출: 1,245 / 5,000 (25%)       │
│                                        │
│ 예상 추가 비용: $0 (한도 내)            │
│                                        │
│ 결제 수단: •••• 1234 (Visa)            │
│ [변경] [영수증 다운로드] [플랜 변경]    │
└────────────────────────────────────────┘
```

---

### 4.3 콘텐츠 관리 및 모더레이션

#### ❌ 부족한 부분
1. **콘텐츠 정책 불명확**
   - 금지 콘텐츠 가이드라인 없음
   - 저작권 정책 불명확
   - 신고 시스템 없음

2. **자동 모더레이션 없음**
   - 부적절한 콘텐츠 필터링 없음
   - AI 기반 검수 없음

3. **콘텐츠 라이센스 관리 부족**
   - 생성된 콘텐츠의 상업적 사용 권한 불명확
   - 워터마크 옵션 불명확

#### ✅ 개선 방안

**A. 콘텐츠 정책 및 모더레이션**
```typescript
interface ContentModeration {
  policies: {
    prohibited: string[]; // 금지 콘텐츠 키워드
    restricted: string[]; // 제한 콘텐츠
    ageRestricted: boolean;
  };
  autoModeration: {
    textFilter: {
      enabled: boolean;
      severity: 'strict' | 'moderate' | 'permissive';
    };
    imageAnalysis: {
      enabled: boolean;
      checkFor: ('violence' | 'nudity' | 'hate-symbols')[];
    };
  };
  reporting: {
    reasons: string[];
    reviewProcess: 'auto' | 'manual' | 'hybrid';
    appealProcess: boolean;
  };
}
```

**B. 라이센스 관리**
```typescript
interface LicenseManagement {
  contentLicense: {
    type: 'personal' | 'commercial' | 'enterprise';
    restrictions: string[];
    attribution: boolean;
  };
  watermark: {
    enabled: boolean;
    position: 'top-right' | 'bottom-right' | 'center';
    opacity: number;
    text: string;
    removable: boolean; // 프리미엄 플랜
  };
  exportOptions: {
    formats: ('mp3' | 'wav' | 'mp4' | 'mov')[];
    maxQuality: string;
  };
}
```

---

## 5. 보안 및 데이터 관리 개선

### 5.1 보안 강화

#### ❌ 부족한 부분
1. **2FA(Two-Factor Authentication) 없음**
   - 추가 인증 방법 없음
   - 보안 강화 옵션 부족

2. **세션 관리 미흡**
   - 동시 로그인 제한 없음
   - 의심스러운 활동 감지 없음
   - 로그인 히스토리 부족

3. **API 보안 미흡**
   - API 키 관리 불명확
   - Rate limiting 상세 없음
   - IP 화이트리스트 없음

#### ✅ 개선 방안

**A. 2FA 시스템**
```typescript
interface TwoFactorAuth {
  methods: {
    totp: {
      enabled: boolean;
      qrCode: string;
      backupCodes: string[];
    };
    sms: {
      enabled: boolean;
      phoneNumber: string;
      verified: boolean;
    };
    email: {
      enabled: boolean;
      verificationCode: string;
    };
  };
  recovery: {
    backupCodes: string[];
    recoveryEmail: string;
  };
}
```

**B. 세션 관리 대시보드**
```
┌────────────────────────────────────────┐
│ 🔐 활성 세션                            │
├────────────────────────────────────────┤
│ 💻 Windows - Chrome                    │
│    서울, 한국                           │
│    현재 세션                            │
│    마지막 활동: 방금 전                 │
│                                        │
│ 📱 iPhone - Safari                     │
│    서울, 한국                           │
│    마지막 활동: 2시간 전                │
│    [세션 종료]                         │
│                                        │
│ 의심스러운 활동                         │
│ ⚠️ 3회 실패한 로그인 시도 (부산, 한국)  │
│    2025-10-06 09:23                    │
│                                        │
│ [모든 세션 종료] [로그인 히스토리]      │
└────────────────────────────────────────┘
```

**C. API 보안 강화**
```typescript
interface APISecuri {
  apiKeys: {
    id: string;
    name: string;
    key: string;
    scopes: string[];
    rateLimit: {
      requestsPerMinute: number;
      requestsPerDay: number;
    };
    ipWhitelist: string[];
    createdAt: Date;
    lastUsed: Date;
  }[];
  security: {
    rateLimiting: {
      global: number;
      perEndpoint: { [endpoint: string]: number };
    };
    cors: {
      allowedOrigins: string[];
      allowedMethods: string[];
    };
    encryption: {
      inTransit: 'TLS1.3';
      atRest: 'AES256';
    };
  };
}
```

---

### 5.2 데이터 프라이버시

#### ❌ 부족한 부분
1. **GDPR 준수 기능 부족**
   - 데이터 다운로드 기능 없음
   - 계정 삭제 시 데이터 처리 불명확
   - 쿠키 동의 관리 없음

2. **데이터 암호화 상세 없음**
   - 클라이언트 사이드 암호화 없음
   - E2E 암호화 없음

3. **감사 로그 부족**
   - 누가 언제 무엇을 했는지 추적 불가
   - 데이터 접근 로그 없음

#### ✅ 개선 방안

**A. GDPR 준수 기능**
```typescript
interface GDPRCompliance {
  dataExport: {
    format: 'json' | 'csv' | 'xml';
    includeProjects: boolean;
    includeSettings: boolean;
    includeActivityLog: boolean;
  };
  rightToErasure: {
    anonymize: boolean;
    deleteAllData: boolean;
    retentionPeriod: number; // days
  };
  cookieConsent: {
    necessary: true;
    analytics: boolean;
    marketing: boolean;
    preferences: boolean;
  };
  privacySettings: {
    trackingOptOut: boolean;
    emailOptOut: boolean;
    dataSharing: 'none' | 'anonymous' | 'full';
  };
}
```

**B. 감사 로그**
```
┌────────────────────────────────────────┐
│ 📋 활동 로그                            │
├────────────────────────────────────────┤
│ 2025-10-06 15:30:45                    │
│ 👤 홍길동 (user@example.com)          │
│ 🔧 프로젝트 "에어컨 요금 폭탄" 수정    │
│ 📍 IP: 192.168.1.1 (서울, 한국)        │
│                                        │
│ 2025-10-06 14:20:12                    │
│ 👤 홍길동                              │
│ 🎤 패널 3 TTS 생성                     │
│ 📍 IP: 192.168.1.1                     │
│                                        │
│ 2025-10-06 10:15:33                    │
│ 👤 홍길동                              │
│ 📁 새 프로젝트 생성                    │
│ 📍 IP: 192.168.1.1                     │
│                                        │
│ [필터] [내보내기] [더 보기]            │
└────────────────────────────────────────┘
```

---

## 6. 성능 및 확장성 개선

### 6.1 확장성

#### ❌ 부족한 부분
1. **대용량 프로젝트 처리 미흡**
   - 100개 이상 패널 처리 전략 없음
   - 무한 스크롤/가상 스크롤 언급 없음

2. **동시 작업 처리 전략 없음**
   - 여러 TTS 동시 생성 시 처리 방법 불명확
   - 큐 관리 전략 부족

3. **확장 가능한 아키텍처 상세 부족**
   - 마이크로서비스 간 통신 프로토콜 불명확
   - 서비스 디스커버리 없음
   - 로드 밸런싱 전략 부족

#### ✅ 개선 방안

**A. 가상 스크롤링**
```typescript
interface VirtualScrolling {
  itemHeight: number | 'auto';
  overscan: number; // 버퍼 아이템 수
  renderCount: number; // 한 번에 렌더링할 아이템 수
  recycleNodes: boolean;
  lazyLoad: {
    enabled: boolean;
    threshold: number; // pixels
  };
}

// 100개+ 패널을 부드럽게 스크롤
<VirtualList
  items={panels}
  itemHeight={120}
  height={600}
  renderItem={(panel) => <PanelItem {...panel} />}
/>
```

**B. 작업 큐 관리**
```typescript
interface JobQueueManagement {
  queue: {
    concurrent: number; // 동시 처리 수
    priority: 'fifo' | 'lifo' | 'priority';
    maxRetries: number;
  };
  status: {
    pending: Job[];
    processing: Job[];
    completed: Job[];
    failed: Job[];
  };
  monitoring: {
    avgProcessingTime: number;
    successRate: number;
    queueDepth: number;
  };
}
```

**C. 마이크로서비스 통신**
```typescript
interface ServiceCommunication {
  protocol: 'gRPC' | 'REST' | 'GraphQL';
  serviceDiscovery: {
    type: 'consul' | 'eureka' | 'kubernetes';
    healthCheck: {
      interval: number;
      timeout: number;
    };
  };
  loadBalancing: {
    strategy: 'round-robin' | 'least-connections' | 'ip-hash';
    stickySession: boolean;
  };
  circuitBreaker: {
    failureThreshold: number;
    timeout: number;
    resetTimeout: number;
  };
}
```

---

## 7. 우선순위 및 구현 순서

### 7.1 MVP (Minimum Viable Product) - Phase 1

**목표**: 핵심 기능으로 빠른 론칭

#### 🔴 Critical (필수)
1. **Undo/Redo 기능** - 사용자 실수 복구 필수
2. **에러 처리 개선** - 안정성 확보
3. **자동 저장 강화** - 데이터 손실 방지
4. **패널 복사/붙여넣기** - 기본 편집 기능
5. **TTS 비용 표시** - 비용 투명성
6. **네트워크 상태 표시** - 사용자 인지

**예상 기간**: 2주

---

### 7.2 Essential Features - Phase 2

**목표**: 사용성 대폭 개선

#### 🟠 High Priority
1. **브레드크럼 네비게이션** - UX 개선
2. **검색 및 필터** - 콘텐츠 관리
3. **일괄 편집 기능** - 생산성 향상
4. **발음 사전** - TTS 품질 향상
5. **프리셋 관리** - 템플릿 활용
6. **버전 관리 (기본)** - 변경 추적
7. **오프라인 지원 (기본)** - 안정성

**예상 기간**: 4주

---

### 7.3 Advanced Features - Phase 3

**목표**: 고급 기능 및 차별화

#### 🟡 Medium Priority
1. **리치 텍스트 에디터** - 고급 편집
2. **오디오 편집 기능** - 품질 향상
3. **이미지 고급 편집** - 시각적 품질
4. **타임라인 에디터** - 전문가용
5. **팀 관리 시스템** - B2B 확장
6. **다양한 음성 옵션** - 다양성
7. **프리셋 마켓플레이스** - 생태계 구축

**예상 기간**: 8주

---

### 7.4 Enterprise Features - Phase 4

**목표**: 엔터프라이즈 및 확장

#### 🟢 Low Priority
1. **실시간 협업** - 팀워크 강화
2. **외부 통합** (YouTube 등) - 워크플로우 통합
3. **개발자 API** - 생태계 확장
4. **고급 분석** - 인사이트 제공
5. **화이트라벨** - B2B 확장
6. **다국어 지원** - 글로벌 확장

**예상 기간**: 12주

---

## 8. 구현 체크리스트

### 8.1 즉시 구현 필요 (MVP)

```
UX/UI
[ ] Undo/Redo 기능 구현
[ ] 통합 에러 알림 시스템
[ ] 로딩 상태 인디케이터
[ ] 자동 저장 강화 (5초 → 3초)
[ ] 패널 복사/붙여넣기
[ ] 네트워크 상태 바

기능
[ ] TTS 비용 계산 및 표시
[ ] 글자 수 카운터
[ ] 기본 검색 기능
[ ] 프로젝트 정렬 (최근순, 이름순)

기술
[ ] 오프라인 감지
[ ] IndexedDB 로컬 저장
[ ] 에러 로깅 (Sentry)
[ ] 성능 모니터링 (기본)
```

### 8.2 단기 구현 (1-2개월)

```
UX/UI
[ ] 브레드크럼 네비게이션
[ ] 고급 검색 및 필터
[ ] 일괄 편집 UI
[ ] 최근 작업 위젯
[ ] 모바일 제스처

기능
[ ] 발음 사전
[ ] 프리셋 저장/관리
[ ] 버전 히스토리 (기본)
[ ] 프로젝트 태그
[ ] 음성 옵션 확장 (3-5개)

기술
[ ] Redis 캐싱 강화
[ ] WebSocket 안정화
[ ] 이미지 최적화 파이프라인
[ ] API Rate Limiting
```

### 8.3 중기 구현 (3-6개월)

```
UX/UI
[ ] 리치 텍스트 에디터
[ ] 타임라인 에디터
[ ] 협업 UI (커서, 잠금 등)
[ ] 고급 설정 패널

기능
[ ] 오디오 편집 (볼륨, 페이드 등)
[ ] 이미지 고급 편집 (필터, 효과)
[ ] 팀 관리 시스템
[ ] 프리셋 마켓플레이스
[ ] 다중 이미지 지원

기술
[ ] 실시간 동기화 (WebSocket)
[ ] Microservice 완전 분리
[ ] Kubernetes 배포
[ ] CDN 최적화
[ ] 전체 오프라인 지원
```

---

## 9. 결론 및 제안

### 9.1 핵심 개선 포인트

1. **사용자 경험 (UX)**
   - ❗ Undo/Redo는 필수 기능
   - ❗ 에러 처리와 피드백 강화 필요
   - ❗ 모바일 UX 대폭 개선 필요

2. **기능 완성도**
   - ❗ 패널 편집 기능 확장 (복사, 일괄 편집 등)
   - ❗ TTS 품질 관리 (발음, 감정 등)
   - ❗ 프리셋 시스템 강화

3. **기술적 안정성**
   - ❗ 오프라인 지원 필수
   - ❗ 실시간 동기화 개선
   - ❗ 성능 최적화 (가상 스크롤 등)

4. **비즈니스 로직**
   - ❗ 구독/결제 시스템 명확화
   - ❗ 사용량 추적 및 제한
   - ❗ 팀 관리 기능

### 9.2 구현 우선순위 요약

**즉시 (2주)**:
- Undo/Redo, 에러 처리, 자동 저장, TTS 비용 표시

**단기 (2개월)**:
- 검색/필터, 발음 사전, 프리셋 관리, 버전 관리

**중기 (6개월)**:
- 고급 편집, 협업, 팀 관리, 마켓플레이스

**장기 (12개월)**:
- 외부 통합, API, 글로벌 확장

### 9.3 권장 사항

1. **MVP 먼저 안정화**: 핵심 기능을 완벽하게 구현
2. **사용자 피드백 수집**: 베타 테스트를 통한 검증
3. **점진적 개선**: 단계별로 기능 추가
4. **성능 모니터링**: 초기부터 메트릭 수집
5. **보안 우선**: 데이터 보호와 프라이버시 강화

---

## 10. 2026-05-04 추가 분석: 영상 편집 UX/기능 설계 재검토

현재 설계와 구현은 "패널 기반 콘텐츠 관리"에는 접근하고 있으나, "영상 제작 편집기"로서 필수적인 편집 피드백 루프가 약합니다. 특히 사용자가 결과물을 확인하면서 수정하는 구조가 부족하므로, 아래 항목을 우선 개선 대상으로 둡니다.

### 10.1 핵심 부족 사항

| 영역 | 현재 문제 | 개선 방향 |
|------|-----------|-----------|
| 영상 프리뷰 | 텍스트 스타일 미리보기만 있고 최종 영상 화면이 없음 | 9:16 `VideoPreviewCanvas`를 중앙 핵심 화면으로 배치 |
| 타임라인 | 패널 순서 변경은 가능하지만 시간/재생 흐름이 없음 | 씬 카드 기반 `SceneTimeline` 추가 |
| TTS | 음성 설정 필드만 있고 실제 생성/상태/재생 흐름 없음 | TTS job, audio_url, duration, 실패 복구 UX 추가 |
| 이미지 | image_url 필드만 있고 업로드/크롭/프리뷰 연결 없음 | Multer + Sharp 업로드와 프리뷰 캔버스 연결 |
| 렌더/export | 완성 결과물을 만드는 단계가 없음 | FFmpeg 기반 render job과 다운로드 UX 추가 |
| 작업 상태 | 저장, 생성, 렌더 상태가 흩어짐 | 상단 toolbar와 toast로 통합 상태 표시 |

### 10.2 재정의된 MVP 판단 기준

MVP 완료 기준은 CRUD 기능 완성이 아니라, 아래 제작 루프가 끊기지 않는 것입니다.

```text
대본 작성
  -> 씬 분할
  -> 이미지 적용
  -> TTS 생성
  -> 9:16 프리뷰 확인
  -> 타임라인 전체 재생
  -> mp4 export
```

### 10.3 우선순위 재조정

즉시:
- 깨진 한글 UI 정리
- 프론트 lint 오류 수정
- 백엔드 SQLite 네이티브 모듈 재빌드
- 패널 reorder API 라우팅 버그 수정

단기:
- 9:16 프리뷰 캔버스
- 씬 타임라인
- 이미지 업로드
- 씬 단위 TTS 생성
- 오디오 플레이어와 duration 계산

중기:
- 전체 재생
- FFmpeg export
- 렌더 작업 상태
- 자동 저장
- 프리셋과 전체 스크립트 자동 분할

장기:
- Undo/Redo
- 발음 사전
- 고급 이미지/오디오 편집
- 협업
- 결제/사용량 제한
- 클라우드 저장소

### 10.4 설계 원칙

1. 프리뷰와 타임라인을 MVP 중심으로 둡니다.
2. 패널은 UI에서 "씬"으로 다루고, DB 전환은 점진적으로 진행합니다.
3. TTS/이미지/렌더는 모두 job 상태를 가져야 합니다.
4. 사용자가 export 가능 여부를 항상 알 수 있어야 합니다.
5. 인프라 확장보다 제작 흐름 완성을 먼저 검증합니다.

---

**문서 끝**

이 문서는 현재 설계의 부족한 부분을 체계적으로 분석하고, 실행 가능한 개선 방안을 제시합니다.

